# customer-support-agent — frontend

React + Vite + TypeScript scaffold, minimal CSS (`aamad.config.yml` `ui.visual_style: minimal`), routing only (`/chat`, `/inbox`, `/ops` placeholders). No real UI yet — see `@frontend.eng`.

For install/run instructions, see [`project-context/2.build/setup.md`](../project-context/2.build/setup.md) (the single source of truth for local setup in this repo).
