/**
 * Single source of truth for the demo hotel's display name, so it's
 * defined once rather than duplicated across every route/component
 * that references it.
 */
export const HOTEL_NAME = "The Wernfield Hotel";
