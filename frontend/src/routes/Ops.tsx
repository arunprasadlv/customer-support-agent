/**
 * /ops route placeholder.
 *
 * Scaffolded by @project.mgr (*setup-project). Real ops view
 * (interaction log + KB review queue) is @frontend.eng's
 * responsibility (*develop-fe) per project-context/1.define/sad.md SS3
 * (NFR-003, FR-014).
 */
export default function Ops() {
  return (
    <section>
      <h1>Ops</h1>
      <p>
        Coming soon — interaction log and KB review queue (see
        @frontend.eng).
      </p>
    </section>
  );
}
