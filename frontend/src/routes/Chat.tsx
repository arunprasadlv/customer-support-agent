/**
 * /chat route placeholder.
 *
 * Scaffolded by @project.mgr (*setup-project). Real chat widget UI is
 * @frontend.eng's responsibility (*develop-fe) per
 * project-context/1.define/sad.md SS3 (guest chat widget, NFR-001).
 */
export default function Chat() {
  return (
    <section>
      <h1>Chat</h1>
      <p>Coming soon — guest chat widget (see @frontend.eng).</p>
    </section>
  );
}
