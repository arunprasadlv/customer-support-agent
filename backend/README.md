# customer-support-agent — backend

Python + FastAPI + CrewAI scaffold (`pyproject.toml`). Only `/health` is implemented — see `@backend.eng` for `InquiryFlow`/`EscalationResolutionFlow`, the 5 agents, and the full API contract.

For install/run instructions, see [`project-context/2.build/setup.md`](../project-context/2.build/setup.md) (the single source of truth for local setup in this repo).
