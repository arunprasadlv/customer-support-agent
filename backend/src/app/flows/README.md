# flows/

Placeholder directory scaffolded by `@project.mgr` (`*setup-project`).

This is where `@backend.eng` implements `InquiryFlow` and
`EscalationResolutionFlow` (CrewAI `Flow` classes) per
`project-context/1.define/sad.md` §1 (ADR-001) and §2
(Task / Turn Orchestration). No flow logic exists yet.
