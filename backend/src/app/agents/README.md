# agents/

Placeholder directory scaffolded by `@project.mgr` (`*setup-project`).

This is where `@backend.eng` implements the CrewAI `Crew` assembly
(the 4-agent reasoning Crew) and the standalone `pii_guard` agent
wiring, reading role/goal/task definitions from
`../config/agents.yaml` and `../config/tasks.yaml`, per
`project-context/1.define/sad.md` §2. No agent logic exists yet.
