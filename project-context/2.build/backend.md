# Backend Build Log — customer-support-agent

## Input Requirements

**PRD**: `project-context/1.define/prd.md`
**SAD**: `project-context/1.define/sad.md` §1 (ADR-001/002/003/004/005), §2 (Multi-Agent System Specification, Task/Turn Orchestration, Typed Task Outputs), §4 (Backend Architecture), §7 (Performance & Scalability), §9 (Testing & QA)
**Setup scaffold**: `project-context/2.build/setup.md`
**Selected Runtime**: `crewai` (`aamad.config.yml` `runtime.target: crewai`, consistent with `AAMAD_TARGET_RUNTIME`)
**Scope of this action**: `*develop-be`, Phase 1 of `sad.md`'s "MVP Build Sequencing" — `InquiryFlow` for `channel=chat` only, callable from Python/pytest. No FastAPI route wiring (`POST /chat` is `*implement-endpoint`'s scope) and no `EscalationResolutionFlow`/Reviewer KB-write path (Phase 2/3).
**Continuation note**: this run continues a prior `*develop-be` invocation that was killed mid-way. `backend/src/app/schemas/task_outputs.py` (the 5 typed Pydantic contracts) and `backend/src/app/domain/loader.py` (`domain_config.json` loader + ADR-005 `kb_search()`) were already complete and correct — read in full before starting, not modified, not duplicated.

## Action Log (`*develop-be`)

### 0. Environment resolution (blocking issue found and resolved)

The repo's shared root `.venv` runs **Python 3.14.5**. `crewai` (all published versions as of this run) requires `Python <3.14` — `pip install -e "./backend[dev]"` against the root `.venv` fails to resolve `crewai>=0.80` for that reason. This was flagged as an open question in `setup.md` ("Whether `@backend.eng` wants the backend `.venv` to remain the shared root `.venv` or split into its own once `crewai`'s dependency footprint is known").

**Resolution**: created a **dedicated `backend/.venv`** using the Python 3.11.16 interpreter already available on the machine (`uv`-managed, at `C:\Users\arunp\AppData\Roaming\uv\python\cpython-3.11.16-windows-x86_64-none\python.exe`), gitignored via the existing root `.venv/` gitignore pattern (matches at any depth). Installed `backend[dev]` there successfully — resolved to **`crewai==1.15.17`**. Added the `anthropic` extra (`crewai[anthropic]`) since this crewai version's native Anthropic provider path requires it (`ImportError: Anthropic native provider not available` otherwise) — `pyproject.toml` updated to `crewai[anthropic]>=1.15` (was `crewai>=0.80`, now realistic against what's actually installable). Also added `pyyaml>=6.0` as an explicit direct dependency (previously only a transitive dependency of crewai; this backend's own `config_loader.py` imports it directly, so declaring it explicitly is correct per aamad-core.md "Deterministic execution").

This backend/.venv split is recorded here as the resolution to that setup.md Open Question — see Audit for tool versions.

### 1. CrewAI version note (`crewai==1.15.17`, not the tutorial-era `@CrewBase` idiom)

The installed crewai version's `crewai.project.CrewBase` decorator auto-maps **every** entry in the configured `agents.yaml`/`tasks.yaml` against the methods defined on a single crew class (`crewai.project.crew_base.map_all_task_variables`) and raises `KeyError` the moment a task/agent name in the shared YAML doesn't have a matching `@agent`/`@task` method on that specific class. Since this run's task list calls for **one shared `config/agents.yaml`** and **one shared `config/tasks.yaml`** containing all 5 agents/tasks (`pii_guard` + the 4 reasoning-Crew members), but `pii_guard` must be built and invoked as a **separate, standalone Crew** from the 4-agent reasoning Crew (ADR-003), using `@CrewBase` as-is would break the moment `ReasoningCrew` tried to load `redact_pii_task`'s `agent: pii_guard` reference (not defined on that class).

Resolution: `backend/src/app/agents/config_loader.py` is a small manual YAML loader + `Agent`/`Task` factory (documented in its own module docstring) that reads the same shared YAML files and builds plain `crewai.Agent`/`crewai.Task` objects directly — config is still fully externalized to YAML (adapter-crewai.md's rule), just resolved by straightforward Python instead of the decorator. This is a deliberate implementation choice recorded here, not a deviation from adapter-crewai.md's intent.

### 2. Files built this run

```
backend/
├── pyproject.toml                       # MODIFIED: crewai>=0.80 -> crewai[anthropic]>=1.15, +pyyaml, +pytest marker
├── src/app/
│   ├── config/
│   │   ├── agents.yaml                  # FILLED IN: 5 agent definitions (role/goal/backstory/llm/temperature/allow_delegation/max_iter/max_retry_limit)
│   │   └── tasks.yaml                   # FILLED IN: 5 task definitions (description/expected_output/agent/context/output_pydantic)
│   ├── tools/
│   │   ├── pii_detector.py              # NEW: deterministic regex/pattern PII detector (pure function)
│   │   └── crewai_tools.py              # NEW: CrewAI `Tool` wrappers around pii_detector.detect_pii and domain/loader.kb_search
│   ├── agents/
│   │   ├── config_loader.py             # NEW: shared YAML loader + Agent/Task factory (see §1 above)
│   │   ├── reasoning_crew.py            # NEW: 4-agent reasoning Crew (Process.sequential)
│   │   ├── pii_guard.py                 # NEW: standalone 1-agent pii_guard Crew (ADR-003)
│   │   └── README.md                    # UPDATED (was placeholder)
│   ├── flows/
│   │   ├── escalation_gate.py           # NEW: pure-function escalation decision (ADR-002)
│   │   ├── inquiry_flow.py              # NEW: InquiryFlow (CrewAI Flow) + run_inquiry() entry point
│   │   └── README.md                    # UPDATED (was placeholder)
│   ├── persistence/
│   │   ├── __init__.py                  # NEW
│   │   └── interaction_log.py           # NEW: SQLite interaction-log store
│   └── schemas/, domain/                # UNCHANGED — predecessor's work (task_outputs.py, loader.py), verified correct, not modified
├── data/                                 # app.db created here at runtime (gitignored, .gitkeep already tracked)
└── tests/
    ├── unit/
    │   ├── test_pii_detector.py         # NEW
    │   ├── test_escalation_gate.py      # NEW
    │   └── test_interaction_log.py      # NEW
    └── integration/
        ├── test_health.py               # UNCHANGED (project-mgr's smoke test)
        └── test_inquiry_flow.py         # NEW
```

`backend/src/app/main.py` was **not** touched (still only `GET /health`) — FastAPI route wiring is explicitly out of scope for this run.

### 3. PII detection — deterministic tool, agent-invoked (stakeholder-confirmed design)

Per the design decision confirmed before this run started: PII detection is a **deterministic regex/pattern-based pure function** (`backend/src/app/tools/pii_detector.py::detect_pii`), same style as `domain/loader.py`'s `kb_search` — no LLM-only detection. It covers, at minimum, the required set:

- `email` — standard local-part@domain.tld pattern.
- `phone` — North-American-style 10-digit numbers, optional country code/separators, bounded by non-digit lookaround.
- `account_number` — digit sequences (6-19 digits) **preceded by an explicit context keyword** (`account`/`acct`/`confirmation`/`reservation`/`booking`/`card`, optionally followed by "number"/"no"/"#" and a connector like "is"/"was"/":"). This keeps the match explainable and deliberately avoids treating bare digit sequences as account numbers (unit-tested).
- `name` — capitalized 1-3 word sequences following a self-introduction phrase (`my name is` / `I am` / `I'm` / `this is`).

Every detected span returns `(entity_type, span_start, span_end, method)` matching `RedactionAction` in `task_outputs.py` exactly (imported, not redefined). Overlapping candidate spans are resolved deterministically by priority order (`email > account_number > phone > name`), then longest-match, then leftmost — never double-redacted (unit-tested).

`pii_guard` (the CrewAI agent, per ADR-003/ADR-004/SAD §2's agent table) is bound to a `crewai.tools.tool`-wrapped version of this function (`crewai_tools.py::pii_detector_tool`) and invoked via its own standalone 1-agent Crew (`agents/pii_guard.py`) — the agent's task/backstory explicitly instruct it to call the tool once and relay the output verbatim, not to re-detect or paraphrase. `InquiryFlow.pii_redact` (step 2) kicks off this Crew and validates the returned type is a real `RedactionResult`; **any exception or malformed output raises `PiiGuardFailure`**, which the Flow does not catch — it propagates out of `kickoff()` (fail-closed, no unredacted text passed forward, per sad.md §2 Error handling / FR-011). A Diagnostic record is persisted to the interaction log before re-raising, and it **never contains the raw text** (a placeholder string is stored instead) — verified by `tests/integration/test_inquiry_flow.py::test_pii_redact_is_fail_closed_on_pii_guard_failure`, which monkeypatches the pii_guard Crew to fail and asserts both the exception and the redacted-placeholder diagnostic record.

### 4. Reasoning Crew + InquiryFlow

- `agents/reasoning_crew.py` builds the 4 reasoning agents from `agents.yaml`, wires `knowledge_retriever`'s task to the `kb_search` tool (ADR-005, `crewai_tools.py::kb_search_tool` wrapping `domain/loader.py::kb_search` unchanged), and context-chains the 4 tasks classify -> retrieve -> sentiment -> compose exactly as `sad.md` §2 step 3 specifies, `Process.sequential`.
- `flows/escalation_gate.py::evaluate_escalation` is a standalone pure function (importable and unit-tested without any Flow/Crew/LLM machinery) implementing the pinned thresholds exactly: `grounded == false OR confidence <= 0.70 OR sentiment_score >= 0.75 -> escalate`. 9 unit tests cover every boundary (exact threshold values, multi-condition combinations, the not-tunable `grounded=false` case).
- `flows/inquiry_flow.py::InquiryFlow` implements all 6 steps from `sad.md` §2 as a CrewAI `Flow` (`@start`/`@listen`/`@router`), state is the framework's default plain-`dict` state (seeded via `kickoff(inputs={channel, raw_text, sender_id})`):
  1. `intake_normalize` (`@start`)
  2. `pii_redact` (`@listen`) — see §3 above
  3. `run_reasoning_crew` (`@listen`) — kicks off the reasoning Crew on `clean_text`, extracts and type-checks each task's `output.pydantic` (raises `TypeError` if any task didn't return its declared contract — "malformed output fails closed", sad.md §9)
  4. `escalation_gate` (`@router`) — thin wrapper calling `evaluate_escalation` on exactly `confidence`/`sentiment_score`/`grounded`
  5. `deliver_response` (`@listen(or_("escalate", "respond"))`) — builds the reply payload (`{escalated: bool, reply: str, reason: list[str] | None}`) as a plain data structure only; **no HTTP/channel adapter** (explicit scope boundary confirmed before this run — that is `*implement-endpoint`'s job)
  6. `log_interaction` (`@listen(deliver_response)`) — always runs on both branches, persists one record

  `run_inquiry(channel, raw_text, sender_id)` is the public entry point for Python/pytest callers.

### 5. `max_execution_time` hard ceiling (sad.md §7) — implemented outside CrewAI's `Flow`, not inside it

The installed CrewAI version's `Flow` class (`crewai.flow.runtime.Flow`) has **no native flow-level execution-time cap** — only `Agent.max_execution_time` exists, a different, per-agent-per-task control, not a whole-flow ceiling. `sad.md` §7 requires "Flow-level `max_execution_time` set to 10s" with automatic degrade-to-escalate on expiry.

Resolution: `run_inquiry()` enforces this itself — it submits `InquiryFlow().kickoff(...)` to a single-worker `ThreadPoolExecutor` and calls `.result(timeout=10)`. On `TimeoutError`, it persists an interaction-log record (`outcome="escalated"`, diagnostic noting the ceiling was exceeded) and returns the same escalation-shaped payload the normal `escalate` branch would produce, without raising. This matches sad.md §7's stated behavior ("a stuck or abnormally slow run degrades automatically to the escalate branch rather than hanging indefinitely") even though it isn't literally a CrewAI `Flow` constructor argument. **Known limitation** (Python/stdlib, not a design gap): the underlying flow thread cannot be forcibly killed once started, so a genuinely hung LLM call continues running in the background after the caller receives the timeout-escalation response — acceptable for MVP single-process/no-real-traffic scope (sad.md §7 "Concurrency: not a design driver for MVP"), flagged in Open Questions below.

Verified by `tests/integration/test_inquiry_flow.py::test_run_inquiry_degrades_to_escalate_on_timeout` (monkeypatches `InquiryFlow.kickoff` to sleep past a 1s test timeout; asserts the escalation payload and the persisted diagnostic record).

### 6. Local persistence (SQLite, sad.md §4)

`backend/src/app/persistence/interaction_log.py` — plain stdlib `sqlite3` (no ORM; one table, no relations). Schema: `id, created_at, channel, sender_id, query_text (clean_text, never raw), intent, confidence, sentiment_score, sentiment_label, match_found, grounded, response_text, outcome ("responded" | "escalated" | "diagnostic_halt"), redaction_count, redaction_actions (JSON), diagnostic`. DB path resolves from an explicit argument, then `INTERACTION_LOG_DB_PATH` env var (used by tests for isolation), then defaults to `backend/data/app.db` (matches `.env.example`'s `DATABASE_URL=sqlite:///./data/app.db` in spirit — see Open Questions re: that env var not being read directly). `list_interactions()` exists for tests and the future `GET /interactions` endpoint (Phase 3, out of scope here).

### 7. Model / temperature / token controls (adapter-crewai.md Logging requirement)

| Agent | Model (ADR-004) | LiteLLM/provider string | Temperature | max_iter | max_retry_limit | allow_delegation |
|---|---|---|---|---|---|---|
| `query_classifier` | `claude-haiku-4-5` | `anthropic/claude-haiku-4-5` | 0.2 | 12 | 2 | false |
| `knowledge_retriever` | `claude-haiku-4-5` | `anthropic/claude-haiku-4-5` | 0.2 | 12 | 2 | false |
| `sentiment_analyzer` | `claude-sonnet-5` | `anthropic/claude-sonnet-5` | 0.2 | 12 | 2 | false |
| `response_composer` | `claude-sonnet-5` | `anthropic/claude-sonnet-5` | 0.2 | 12 | 2 | false |
| `pii_guard` | `claude-haiku-4-5` | `anthropic/claude-haiku-4-5` | 0.2 | 12 | 2 | false |

`temperature: 0.2` was set explicitly for all 5 agents (CrewAI's `LLM.temperature` default is `None`, i.e. unpinned/provider-default) — none of these tasks are open-ended creative writing (even `response_composer` is constrained to only restate retrieved KB facts, AC-003), so low temperature favors reproducibility over variety, per `aamad-core.md` "Deterministic execution" and `adapter-crewai.md`'s Quality Gate ("Keep artifact-generation tasks deterministic ... low temperature unless justified"). This is a `@backend.eng` judgment call, not a `sad.md`-pinned value — flagged in Assumptions.

`Process.sequential` for the reasoning Crew; `max_iter<=12`/`max_retry_limit>=2`/`allow_delegation=false` for all 5 agents, per `adapter-crewai.md` baselines and `sad.md` §2 Runtime-Conditional Configuration. No agent uses Opus 5 (matches ADR-004 — no current role needs it).

### 8. Tests and verification performed this run

Ran inside `backend/.venv` (Python 3.11.16), from `backend/`:

```
pytest         -> 28 passed, 4 skipped (10.2s)
ruff check .   -> 2 pre-existing E501 nits in predecessor files (task_outputs.py, domain/loader.py) — not modified, see Assumptions
mypy src       -> Success: no issues found in 16 source files
```

Test breakdown:
- **Unit** (`tests/unit/`): `test_pii_detector.py` (9 tests — email/phone/account-number/name detection, offset correctness, no-false-positive-on-bare-digits, overlap resolution, typed-contract check), `test_escalation_gate.py` (9 tests — all threshold boundaries per sad.md §9's "directly unit-testable" callout), `test_interaction_log.py` (4 tests — SQLite persistence round-trip).
- **Integration** (`tests/integration/`): `test_inquiry_flow.py` — 2 deterministic, monkeypatched tests requiring no network/API key (pii_guard fail-closed behavior; `max_execution_time` timeout degrade), plus 4 parametrized true end-to-end tests (one raw-text example per FR-013 scenario category: reservations & booking, check-in/check-out & billing, room service & amenities, general complaints) that call the real `InquiryFlow` through live Anthropic API calls — these are marked `@pytest.mark.skipif(not ANTHROPIC_API_KEY)` and **were skipped in this run** because no `ANTHROPIC_API_KEY` is available in this environment (no `backend/.env` present, no key in the shell). See Open Questions.

No lint/type/test failures were left unresolved; the two remaining `ruff` E501 lines belong to `task_outputs.py`/`domain/loader.py` (predecessor's files, confirmed correct and explicitly not to be modified/diverged from per this run's instructions) and are pre-existing, not introduced by this run.

### 9. Phase 1 — `*implement-endpoint` (`POST /chat`)

**Scope of this action**: expose `run_inquiry()` over HTTP as `POST /chat` only, per sad.md §4's API contract and this run's explicit boundary — `POST /email`, `POST /escalations/{id}/resolve`, and the review-queue endpoints remain Phase 2/3, not touched.

**Files changed**:

- `backend/src/app/main.py` — added `POST /chat` directly in the existing entrypoint (no separate router module; a single route didn't warrant one). Added: `ChatRequest {message: str, session_id: str}` / `ChatResponse {reply: str, escalated: bool}` Pydantic models (sad.md §4 schema, matched exactly); `load_dotenv()` at module import time (backend/.env now exists with a real `ANTHROPIC_API_KEY` — `python-dotenv` was already a declared dependency but previously unused by any code path); a `ChatProcessingError` exception + registered `@app.exception_handler` that maps any unexpected exception surfacing out of `run_inquiry()` to the sad.md §4 `{error_code, message}` envelope (`error_code: "chat_processing_failed"`) with HTTP 500 — the route handler's own `try/except Exception` is what raises `ChatProcessingError`, logging the real traceback server-side via `logger.exception(...)` first so nothing is silently lost. `run_inquiry`'s own internal timeout/escalation degradation (sad.md §7) is untouched and still returns a normal 200 `{reply, escalated: true}` payload, not an error — the exception handler only fires for genuinely unexpected failures (e.g., the reasoning Crew raising instead of degrading, see verification below).
- `backend/tests/integration/test_chat_endpoint.py` — new. Two deterministic 422-validation tests (missing `message` / missing `session_id`, no LLM/network) plus one live end-to-end test (`test_chat_end_to_end_returns_reply_and_escalated`, skipped without `ANTHROPIC_API_KEY`, same skip pattern as `test_inquiry_flow.py`'s existing live tests) that posts through the real `TestClient(app)` HTTP layer and asserts `{reply: str, escalated: bool}`.

**Mapping confirmed against `inquiry_flow.py`**: `run_inquiry(channel, raw_text, sender_id, timeout_seconds=10)` — `message → raw_text`, `session_id → sender_id`, `channel="chat"` hardcoded (this endpoint only ever triggers the chat channel per sad.md §4's `POST /chat` row). Read in full before wiring; no signature mismatch.

**Live verification performed this run** (real Anthropic API calls, `backend/.venv`, `ANTHROPIC_API_KEY` present in `backend/.env`):

- `pytest tests/integration/test_chat_endpoint.py` — 3 passed (2 validation, 1 live end-to-end round trip returning a real composed reply with `escalated: false`).
- Started `uvicorn app.main:app --port 8010 --app-dir src` and sent real `curl` requests: `GET /health` → 200; `POST /chat` with a missing `message` field → 422 (Pydantic validation, not a 500); `POST /chat` with a real room-service/reservations message → in different calls, either a normal 200 `{reply, escalated}` or (see below) a clean 500 `{error_code, message}` envelope — both are correct, expected outcomes of the wrapping added here, not a wiring defect.
- **Full backend `pytest` suite** (33 passed baseline; 4 additional live scenario tests are flaky under live conditions — see Open Questions below) confirms no regression to any pre-existing test.

**Finding — not fixed, flagged for follow-up (out of scope for this action)**: live calls intermittently (roughly half the time, across the reasoning Crew's `claude-haiku-4-5` calls specifically — `query_classifier`/`knowledge_retriever`) fail with `anthropic.BadRequestError: Error code: 400 - {'message': '`temperature` is deprecated for this model.'}`, even though `pii_guard` (same model/temperature) succeeded moments earlier in the same run. This is a pre-existing `agents.yaml` config detail from `*develop-be` (`temperature: 0.2` set for all 5 agents, already flagged in this file's Assumptions/Open Questions as a `@backend.eng` judgment call needing `@qa.eng`/stakeholder review) — not introduced by this action, and out of `*implement-endpoint`'s scope to fix (route wiring only). When it happens, it is exactly the case the new `ChatProcessingError` handler exists for: the client received a clean `{"error_code":"chat_processing_failed","message":"..."}` at HTTP 500, and the real traceback was logged server-side only — confirmed directly from the `uvicorn` log during this run's live verification. Recommended next step: `@qa.eng`/`@backend.eng` should re-run the §7 Latency Spike's 20+-request sample with this in mind — if it reproduces consistently, dropping the explicit `temperature` override for the Haiku-tier agents (or gating it per-model) in `agents.yaml` is the likely fix, not anything in the endpoint layer.

**Update (2026-08-20) — root cause corrected and fixed. See "10. `*implement-endpoint` follow-up fix" below.** The original attribution above (blaming the Haiku-tier agents) was a misdiagnosis made under intermittent/concurrent conditions — a direct reproduction shows the opposite: `claude-sonnet-5` is the model that rejects `temperature`, not `claude-haiku-4-5`.

### 10. `*implement-endpoint` follow-up fix (2026-08-20) — `temperature` 400 on `claude-sonnet-5`, corrected root cause

**Trigger**: a live `POST /chat` call failed with `anthropic.BadRequestError: temperature is deprecated for this model` during `*implement-endpoint` verification, blocking the SAD §7 Latency Spike (which needs clean, error-free live calls to produce valid measurements).

**Investigation**:

- Read `crewai==1.15.17`'s native Anthropic provider (`backend/.venv/Lib/site-packages/crewai/llms/providers/anthropic/completion.py`) — this project's installed CrewAI version uses the `anthropic` Python SDK directly (via the `crewai[anthropic]` extra), **not** LiteLLM, for Anthropic calls (`AnthropicCompletion._prepare_completion_params`: `if self.temperature is not None: params["temperature"] = self.temperature`). No LiteLLM package is even installed in `backend/.venv` — confirmed (`ModuleNotFoundError: No module named 'litellm'`). CrewAI's own code has **no** model-specific guardrail around `temperature` — it forwards whatever is configured unconditionally. The rejection is therefore a **live Anthropic API-side behavior**, not a CrewAI/LiteLLM client-side validation quirk; nothing in the installed `anthropic==0.73.0` SDK validates or blocks `temperature` client-side either (grepped, no matches).
- Reproduced directly against the live Anthropic API (bypassing CrewAI entirely, `backend/.venv`, real `ANTHROPIC_API_KEY` from `backend/.env`), calling `client.messages.create(...)` with `temperature=0.2` for both models, 3 repetitions each:
  - `claude-haiku-4-5` **with** `temperature=0.2` → **OK**, 3/3.
  - `claude-sonnet-5` **with** `temperature=0.2` → **`anthropic.BadRequestError: Error code: 400 - {'type': 'invalid_request_error', 'message': '\`temperature\` is deprecated for this model.'}`**, 3/3 (100% reproducible, not intermittent).
  - Both models succeed with `temperature` omitted entirely.
- **Root cause, corrected**: it is `claude-sonnet-5` (used by `sentiment_analyzer` and `response_composer`) that rejects the `temperature` parameter on the live Anthropic API today — the exact opposite of what the `*implement-endpoint` action's live-verification note (§9 above) attributed it to. `claude-haiku-4-5` (`query_classifier`, `knowledge_retriever`, `pii_guard`) has no such restriction and accepts `temperature` normally. The original note's attribution to the Haiku-tier agents was a misdiagnosis made from ad hoc/concurrent live traffic where it wasn't possible to isolate which of the 5 concurrent-ish agent calls actually produced the error; a direct, isolated, repeated probe against the live API resolves the ambiguity definitively.

**Fix applied** (`backend/src/app/config/agents.yaml` only):

- `temperature: 0.2` is now set **only** on the 3 `claude-haiku-4-5` agents (`query_classifier`, `knowledge_retriever`, `pii_guard`) — unchanged from before.
- `temperature: 0.2` is **removed** (omitted, not set to `None`) on the 2 `claude-sonnet-5` agents (`sentiment_analyzer`, `response_composer`) — these now fall back to CrewAI's/Anthropic's provider default (unpinned), which is required for a successful live call against `claude-sonnet-5` today.
- `backend/src/app/agents/config_loader.py::build_agent` already handled an omitted `temperature` key correctly with no change needed (`if "temperature" in cfg: llm = LLM(model=cfg["llm"], temperature=cfg["temperature"])` — when the key is absent, `llm` stays the plain model string, which uses the provider default).
- The file's explanatory header comment block (lines 1-25 previously) was rewritten to state the corrected per-model reasoning instead of the stale "temperature: 0.2 for all 5 agents" claim, and each Sonnet-tier agent entry has an inline comment noting why `temperature` is intentionally absent.
- The original low-temperature *rationale* (aamad-core.md "Deterministic execution", adapter-crewai.md's Quality Gate) is unchanged and still applies to the 3 Haiku-tier agents; for the 2 Sonnet-tier agents it is now an unavoidable live-API constraint, not a reproducibility downgrade chosen by `@backend.eng`.

**Live verification performed** (real Anthropic API calls, `backend/.venv`, real `ANTHROPIC_API_KEY` from `backend/.env`):

- Started `uvicorn app.main:app --port 8010 --app-dir src` and sent 4 real `POST /chat` requests spanning all 4 seeded `domain_config.json` scenario categories (reservations_booking, checkin_checkout_billing, room_service_amenities, general_complaints). All 4 returned clean `200 {"reply": ..., "escalated": ...}`. Server log (`INFO ... "POST /chat HTTP/1.1" 200 OK` x4) shows **zero** `temperature is deprecated` errors and zero other exceptions — the only log noise is a pre-existing, unrelated Windows-console `charmap`/emoji encoding warning from CrewAI's own event-bus logging (`UnicodeEncodeError` on `\U0001f30a` etc. when printing to a cp1252 console), not an API or application error, not touched (out of scope).
- All 4 of those HTTP-level requests happened to resolve via the existing 10s `max_execution_time` timeout-escalation path (pre-existing/documented behavior, sad.md §7 — see below), so to positively confirm the underlying LLM calls (all 5 agents, both tiers) succeed with no error, `InquiryFlow().kickoff(...)` was also invoked **directly** (bypassing the 10s wrapper) twice, capturing full stdout/stderr:
  - Reservations scenario ("Hi, I would like to book a deluxe king room for 2 nights next weekend..."): completed in **14.98s**, `escalated: False`, real composed reply returned. No errors in captured stderr (`temperature`/`deprecat`/`badrequest`/`error` all absent).
  - General-complaints scenario ("This is the third time I have called about the noise... extremely frustrated."): completed in **16.05s**, `escalated: True`, `reason: ["high_frustration"]` (a real escalation-gate decision, not a timeout/error). No errors in captured stderr.
  - Both runs exercise all 5 agents (`pii_guard` + `query_classifier`, `knowledge_retriever`, `sentiment_analyzer`, `response_composer`) against the real API, confirming the fix holds for the full pipeline, not just an isolated probe.
- `pytest tests/integration/test_chat_endpoint.py` — 3/3 passed (2 validation, 1 live end-to-end), no temperature errors.
- Full `pytest` suite — 31 passed, 4 failed. The 4 failures are all `test_inquiry_flow.py::test_inquiry_flow_end_to_end_per_scenario_category[...]` (one per FR-013 scenario category), and **none are temperature-related** — see "New finding, not fixed" below.

**New finding, not fixed (separate from the temperature bug, flagged for follow-up)**: the 4 failing `test_inquiry_flow_end_to_end_per_scenario_category` tests all fail on `assert len(rows) == 1` — `list_interactions()` returns **2** rows instead of 1. Root cause traced to `run_inquiry()` (`backend/src/app/flows/inquiry_flow.py`): its `with ThreadPoolExecutor(max_workers=1) as pool:` block calls `pool.submit(...)` then `future.result(timeout=10)`; on `FutureTimeoutError` it writes an escalation-diagnostic log row and returns — but that `return` is still inside the `with` block, so Python's context-manager exit (`ThreadPoolExecutor.__exit__` → `shutdown(wait=True)`, the default) blocks until the background `InquiryFlow().kickoff()` thread actually finishes before the function call truly returns to its caller. Now that this fix makes every LLM call succeed, a full 5-agent sequential live run reliably takes ~15-16s (see the two direct-kickoff timings above), consistently exceeding the 10s ceiling — so the background thread keeps running past the timeout, reaches its own `log_interaction` step, and writes a **second** interaction-log row for the same inquiry (in addition to the one written in the `except FutureTimeoutError` branch). This is a **pre-existing defect in `run_inquiry()`'s timeout-handling code**, already partially flagged in this file's existing Open Questions ("`max_execution_time`'s un-killable background thread on timeout... would need a real cancellation strategy... before any concurrent/production use") — but the specific mechanism (the `with` block's `shutdown(wait=True)` silently defeating the intended "return quickly on timeout" behavior, and causing duplicate log rows) was not previously confirmed, because the pre-existing temperature bug caused enough live calls to fail fast (well under 10s) that this path wasn't reliably exercised. It is **not** part of the temperature bug and is explicitly **not fixed here** (out of this action's scope — see task boundary "no scope creep into Phase 2/3"). Flagged in Open Questions below for `@backend.eng`/`@qa.eng` follow-up before the §7 Latency Spike is run, since the spike will now reliably hit this path (real calls no longer error, but real latency exceeds the ceiling).

### 11. `run_inquiry()` double-log fix (2026-08-20) — blocking `shutdown(wait=True)` and duplicate interaction-log rows on timeout, resolved

**Scope**: narrow follow-up fix to the exact defect flagged in §10 above ("New finding, not fixed") and in Open Questions. Touches only `backend/src/app/flows/inquiry_flow.py`, `backend/src/app/persistence/interaction_log.py`, and `backend/tests/integration/test_inquiry_flow.py`. No changes to `POST /chat`, `agents.yaml`, escalation thresholds, or the 10s SAD §7 ceiling value/timeout-escalation behavior itself.

**Two independent problems, two independent fixes**:

1. **Caller blocks past the ceiling.** `run_inquiry()` previously wrapped `pool.submit(...)` / `future.result(timeout=...)` in `with ThreadPoolExecutor(max_workers=1) as pool:`. Python's context-manager `__exit__` calls `pool.shutdown(wait=True)` unconditionally — even when reached via the `except FutureTimeoutError` branch's `return` — so the function did not actually return to its caller until the abandoned background flow thread finished on its own (~15-16s observed), not at the nominal ceiling. **Fix**: the pool is now created and torn down manually (no `with` block); a `finally: pool.shutdown(wait=False)` replaces the implicit `wait=True` shutdown. `shutdown(wait=False)` releases the executor's own bookkeeping without blocking — it does **not**, and cannot, stop the already-running background thread (Python has no API to forcibly kill a running thread; this is the same stdlib limitation already documented in this file's Assumptions/Open Questions, not something this fix tries to solve). The net effect is exactly the one required: `run_inquiry()` itself no longer blocks on that thread's completion.

2. **Duplicate interaction-log row.** Because the background thread cannot be killed, on a real timeout it keeps running and eventually reaches its own `log_interaction` step, which used to call `record_interaction()` with a brand-new `uuid4()` — a second, distinct row for the same inquiry, in addition to the row `run_inquiry()`'s own `except FutureTimeoutError` branch already wrote. **Fix (chosen approach: shared id + idempotent write, not a schema change)**: `run_inquiry()` now mints one `inquiry_id = str(uuid.uuid4())` per call and passes it into `InquiryFlow().kickoff(inputs={..., "inquiry_id": inquiry_id})` — CrewAI's `Flow._initialize_state` merges non-`id` `inputs` keys directly into `self.state` (confirmed by reading `crewai==1.15.17`'s `Flow.kickoff_async`/`_initialize_state` source; note CrewAI's `Flow` already treats a literal `"id"` input key specially for its own state-persistence/restore mechanism, which is exactly why the new key is named `inquiry_id`, not `id`, avoiding any collision with that unrelated CrewAI feature). `InquiryFlow.log_interaction` now uses `self.state.get("inquiry_id") or str(uuid.uuid4())` (the fallback keeps direct `InquiryFlow().kickoff()` callers — e.g. the fail-closed pii_guard test — working unchanged, since they don't seed `inquiry_id`). `interaction_log.py::record_interaction`'s `INSERT` was changed to `INSERT OR IGNORE`, keyed on the existing `id` `PRIMARY KEY` column — **no schema change**. For every normal call site each row still gets a fresh UUID, so this is a no-op change in behavior for all of them; the one deliberate exception is exactly this timeout/straggler race, where `OR IGNORE` makes whichever of the two same-`id` writes lands second a silent no-op instead of a duplicate row — first-writer-wins, order-independent (verified: the synchronous timeout-path write always lands first in practice since it fires at the ceiling, well before the straggler's real completion, but `OR IGNORE` makes the guarantee hold regardless of ordering).

**Why this approach over the alternatives**: an `UPDATE`/upsert-on-conflict (overwriting the timeout row with the straggler's real completion data) was rejected — it would silently rewrite what the caller was actually told (a timeout escalation) into a "responded"/different-outcome row well after the response was already returned, which is worse for NFR-006 auditability, not better: the log should reflect what happened for *that* request/response pair, and the request/response pair the caller received *was* the timeout escalation. A cooperative-cancellation/kill-the-thread approach was explicitly out of scope (stdlib limitation, called out in the task). `INSERT OR IGNORE` keeps the existing one-row-per-`id` schema invariant, requires no migration, and directly encodes "first write for this inquiry wins" — the same semantics as the diagnostic_halt path already uses (a single record per failure mode).

**Test-file change and why**: `test_inquiry_flow_end_to_end_per_scenario_category`'s original `assert len(rows) == 1` counted the *entire* db's rows. Making the two fixes above landed the test at **35 passed** on a clean, isolated single-test run, but the full 4-test parametrized suite still intermittently failed that specific assertion — not because of a dedup failure (verified: with `INSERT OR IGNORE`, at most one row per `inquiry_id` always held), but because of *cross-test* contamination: each parametrized case uses a fresh `tmp_path`-scoped `INTERACTION_LOG_DB_PATH` via `monkeypatch.setenv`, which pytest restores at test teardown; a *prior* test case's still-running, un-killable straggler thread reads `INTERACTION_LOG_DB_PATH` from the environment at write time (not at submission time), so if it finally writes while a *later* test's monkeypatch is active, its row (a different `inquiry_id`, unrelated scenario) lands in that later test's db, inflating its row count. This is a real consequence of the documented "cannot forcibly kill a running thread" limitation surfacing through pytest's per-test env-var monkeypatching, not a flaw in the dedup mechanism. Fix: `run_inquiry()`'s return dict now always includes `inquiry_id` (harmless additive key — `main.py`'s `POST /chat` handler already builds `ChatResponse` from only `result["reply"]`/`result["escalated"]` and ignores extra keys, so this is not an API contract change), and the test now asserts `len([r for r in rows if r["id"] == result["inquiry_id"]]) == 1` instead of `len(rows) == 1` — the same per-inquiry guarantee, without being sensitive to an unrelated scenario's leftover row from a straggler thread outliving its own test's fixture scope.

**Live verification performed** (real Anthropic API calls, `backend/.venv`, real `ANTHROPIC_API_KEY` from `backend/.env`, `PYTHONUTF8=1` — see note below):

- Full `pytest` suite: **35 passed**, 0 failed (was 31 passed / 4 failed before this fix) — all 4 previously-failing `test_inquiry_flow_end_to_end_per_scenario_category[...]` cases now pass, and none of the other 31 regressed.
- `ruff check src tests` — clean on every file touched this fix (`inquiry_flow.py`, `interaction_log.py`, `test_inquiry_flow.py`); the same 2 pre-existing E501 nits in untouched predecessor files (`task_outputs.py`, `domain/loader.py`) remain, unrelated to this fix.
- `mypy src` — `Success: no issues found in 16 source files`.
- Direct live dedup + timing proof (bypassing pytest, `timeout_seconds=3` against a real ~14s full 5-agent flow, `record_interaction` instrumented with a call-count/timestamp spy): `run_inquiry()` returned in **4.59s** wall-clock (not 3s exactly — see note below — but nowhere near the ~14s+ full flow duration the pre-fix code would have blocked for). `record_interaction` was called **twice** with the identical `inquiry_id` — once at `t+4.56s` (the timeout path) and once at `t+13.85s` (the abandoned background thread's own `log_interaction`, confirming it really did keep running and really did reach that step) — and exactly **one row** was persisted in the database, confirming `INSERT OR IGNORE` deduped the second write. Repeated with a second scenario with the same result (1 db row, 2 `record_interaction` calls, early return).
- **Note on the ~1.5-2s overshoot past `timeout_seconds`**: `future.result(timeout=N)` in this Windows/`backend/.venv` (Python 3.11.16) environment consistently returns a few hundred ms to ~2s after `N`, even for a trivial one-step `Flow` with no LLM calls at all (isolated, reproduced independently of this fix: a bare single-`@start`-method `Flow` that just `time.sleep()`s shows the same ~1.5-2s overshoot over the requested `future.result(timeout=...)` value). This traces to CrewAI's own synchronous console/event-bus rendering (`rich`-based flow-started/method-execution panels) running on the background thread and measurably delaying when the main thread's `Condition.wait(timeout)` gets CPU time under CPython's GIL — a pre-existing, environment-specific overhead already noted elsewhere in this file as unrelated console noise (§9/§10's `charmap`/emoji `UnicodeEncodeError` warnings; `PYTHONUTF8=1` removes the encoding errors themselves but not this scheduling overhead, confirmed by testing both ways). It is **not** something this fix introduces or is scoped to address, and it does not change the fix's correctness: at the real `timeout_seconds=10` default, a `run_inquiry()` call now returns at roughly 10-12s instead of blocking for the full ~15-16s real flow duration — the defect (blocking until real completion) is resolved even though the ceiling itself isn't hit with sub-second precision in this dev environment.

### 12. `*implement-endpoint` follow-up — `GET /interactions` (2026-08-20), a deliberate Phase 3 -> now pull-forward

**This is a scope exception, not a scope creep.** `sad.md` §4's API Architecture defines the contract line verbatim: `GET /interactions` — interaction log for the ops view." `sad.md`'s own "MVP Build Sequencing" table scopes this endpoint to **Phase 3** (alongside the rest of the `/ops` view — `GET/POST /review-queue/...`). The operator explicitly authorized building this one endpoint now, ahead of that sequencing, to satisfy a reviewer comment requiring concrete proof that `POST /chat` produces a real, visible interaction-log row before `@integration.eng` wires up the frontend — not a reassessment of the sequencing itself. No other Phase 2/3 endpoint (`POST /email`, `POST /escalations/{id}/resolve`, `GET/POST /review-queue/...`) is touched by this action, and none should be inferred as also pulled forward.

**Files changed**:

- `backend/src/app/main.py` — added `GET /interactions`, registered directly alongside `POST /chat` (no separate router module, consistent with the existing single-file convention noted in Assumptions above). New `InteractionRecord` Pydantic response model (`response_model=list[InteractionRecord]`) mirrors `interaction_log.py`'s `_SCHEMA` columns exactly (`id, created_at, channel, sender_id, query_text, intent, confidence, sentiment_score, sentiment_label, match_found, grounded, response_text, outcome, redaction_count, redaction_actions, diagnostic`), so FastAPI generates a real OpenAPI schema and validates responses rather than returning untyped dicts. Two narrow, documented typing choices beyond a literal column copy: `match_found`/`grounded` are `bool | None` (the column is `INTEGER`, but `record_interaction` already stores `int(bool(...))` — this restores the semantic type on the way out) and `redaction_actions` is `list[dict[str, Any]]` (the column is `TEXT` holding a JSON string; the route `json.loads()`s it before constructing the model — a JSON-string-typed API field would defeat the point of a typed response model). No query params, pagination, or filtering — `sad.md`'s contract line specifies none for MVP and none were invented. The route calls `list_interactions()` directly (already existed, already most-recent-first) with no new persistence logic.
- `backend/src/app/persistence/interaction_log.py` — no functional change; updated `list_interactions()`'s stale docstring (previously said "future `GET /interactions` endpoint (Phase 3, out of scope for this run)") to reflect that it's now used by that endpoint, with a pointer to this section.
- `backend/tests/integration/test_chat_endpoint.py` — extended (not a new file, consistent with this run's guidance) with two tests: `test_interactions_empty_list_when_no_interactions` (deterministic, no LLM/network — asserts `[]` on an empty log) and `test_interactions_shows_record_after_chat_call` (live, `@pytest.mark.skipif(not ANTHROPIC_API_KEY)`, same skip pattern as the existing live `/chat` test — posts a real message through `POST /chat`, then asserts the resulting row is visible via `GET /interactions` with matching `channel`/`sender_id`/`outcome`/`response_text`). The live test filters by `sender_id` rather than asserting a total row count, for the same documented reason as `test_inquiry_flow.py`'s per-`inquiry_id` assertion (§11 above) — an unrelated prior live test's un-killable straggler background thread can land its own row in this test's `tmp_path`-scoped db. It also branches on whether the row carries a `diagnostic` value: the sad.md §7 timeout-escalation path (`run_inquiry`'s `except FutureTimeoutError` branch) persists only a partial row (no `response_text`/`intent`/etc.) and returns a `reply` that was never itself written to the DB — asserting exact `response_text` equality unconditionally would be wrong on that path, and this run's own live verification (below) hit exactly that path once.

**Live verification performed** (real Anthropic API calls, `backend/.venv`, real `ANTHROPIC_API_KEY` from `backend/.env`):

- Started `uvicorn app.main:app --port 8020 --app-dir src` against a fresh `backend/data/app.db`.
- `GET /interactions` before any chat activity → `200 []`.
- `POST /chat` with a real check-in/check-out question (`sender_id: guest-live-verify-1`) → `200 {"reply": "Thanks for your patience. This is taking longer than expected...", "escalated": true}` — this hit the sad.md §7 `max_execution_time` timeout-escalation path (the reasoning Crew's real ~15-16s latency still exceeds the 10s ceiling, per §11's still-open finding above), not an error.
- `GET /interactions` afterward → returned exactly that one row, `id` matching, `sender_id: "guest-live-verify-1"`, `channel: "chat"`, `outcome: "escalated"`, `diagnostic: "InquiryFlow exceeded max_execution_time=10s hard ceiling (sad.md §7)"`, `response_text: null`, `intent`/`confidence`/`sentiment_score`/`match_found`/`grounded`: all `null` — correctly reflecting that the timeout branch never reaches the reasoning Crew's typed outputs. `redaction_actions` came back as a real JSON array (`[]`), not a raw string, confirming the `json.loads()` typing fix works.
- To also prove the non-timeout "responded" path (populated `intent`/`confidence`/`sentiment`), called `run_inquiry(..., timeout_seconds=30, sender_id="guest-live-verify-2")` directly (bypassing the 10s wrapper only for this verification step, same technique as §10/§11's prior live verifications) with the same check-in/check-out question → real composed reply, `escalated: false`. `GET /interactions` then returned **two** rows, most-recent-first: the new one with `intent: "checkin_checkout_billing"`, `confidence: 0.95`, `sentiment_score: 0.05`, `sentiment_label: "neutral"`, `match_found: true`, `grounded: true`, `outcome: "responded"`, `response_text` matching the real composed reply verbatim, `diagnostic: null` — and the earlier timeout row unchanged. Both rows' fields match exactly what each underlying call actually did.
- `pytest tests/integration/test_chat_endpoint.py` and the full `pytest` suite — see Audit below.

### 13. Phase 2 of MVP Build Sequencing (2026-08-21) — `channel=email`, `EscalationResolutionFlow`, review queue, `POST /email`, `POST /escalations/{id}/resolve`

**Scope of this action**: `sad.md`'s "MVP Build Sequencing" Phase 2, exactly: (1) `channel=email` on `InquiryFlow`; (2) `EscalationResolutionFlow` writing a candidate KB entry to a new, write-only `review_queue` store, linked to the original query (FR-008/AC-010); (3) `POST /email` and `POST /escalations/{id}/resolve`. **Not** built (explicitly Phase 3): `GET /review-queue`, `POST /review-queue/{id}/approve`, `POST /review-queue/{id}/reject`, and no live-KB (`domain_config.json`/`knowledge_base`) mutation of any kind (NFR-008 stays intact — nothing in this phase can write the live KB).

**Latency-spike gate note (repeated per this run's instructions)**: the mandatory §7 Latency Spike gate between Phase 1 and Phase 2 has still not been run as a proper 20+-request sample (already flagged in Open Questions since Phase 1/`*implement-endpoint`). This run proceeded into Phase 2 anyway per explicit operator decision, not because the gap is resolved — see Open Questions (carried forward, not re-litigated here). Live verification below reproduced the already-known consequence directly: the real reasoning-Crew pipeline still commonly exceeds the 10s `max_execution_time` ceiling, so `escalated: true` responses seen in this phase's live checks are a mix of genuine sentiment/confidence-driven escalations and timeout-driven ones — both are legitimate `outcome: "escalated"` interaction-log rows either way, and both were exercised (see Live verification).

#### 13.1 `channel=email` — no `InquiryFlow` change needed

Read `flows/inquiry_flow.py::intake_normalize` in full before starting, per this run's instructions: it already reads `channel` generically off `self.state.get("channel", "chat")` — no hardcoded `"chat"` anywhere in the Flow itself (the string `"chat"` there is only a default fallback for callers that don't pass one). `escalation_gate`, `deliver_response`, and `log_interaction` are likewise channel-agnostic (`decision`/`payload` carry no channel-specific branching). So this item resolved to **zero changes in `inquiry_flow.py`** — confirmed by reading, not assumed, per this run's explicit instruction — and was entirely an API-layer change (`POST /email`, §13.3 below), exactly as the task description anticipated ("this may mostly be about the API layer, not the Flow itself").

#### 13.2 `EscalationResolutionFlow` + review queue (new files)

- `backend/src/app/persistence/review_queue.py` — new. Plain stdlib `sqlite3` (no ORM), mirrors `interaction_log.py`'s structure exactly (schema-in-`CREATE TABLE IF NOT EXISTS`, `_connect`/`_resolve_db_path` contextmanager pattern, `INSERT OR IGNORE` keyed on `id`). One new table, `review_queue`, in the **same** `backend/data/app.db` file as `interaction_log` (sad.md §4 Data Architecture: "one local SQLite store" grouping the interaction log, KB, and review queue) — no new db file, no new env var: it deliberately reuses `INTERACTION_LOG_DB_PATH` for path resolution/test isolation rather than introducing e.g. `REVIEW_QUEUE_DB_PATH`, since both tables always live in the one file (see Assumptions). Schema: `id, created_at, original_inquiry_id, original_query_text, resolution_text, candidate_intent, candidate_section, candidate_keywords (JSON), candidate_content, status (default "pending")`. `record_review_queue_entry()` is the only writer used this phase; `list_review_queue()` exists for tests and as the ready read path for Phase 3's `GET /review-queue` (not registered as a route here).
- `backend/src/app/persistence/interaction_log.py` — added `get_interaction_by_id(inquiry_id, db_path=None) -> dict | None` (read-only addition, no schema/behavior change to existing functions). Confirms this run's task instruction directly: the column is `id` (the same id `InquiryFlow`/`run_inquiry()` already persist per inquiry, per the Phase-1 double-log fix, §11 above) — `original_inquiry_id` in the new flow/endpoint is exactly this `interaction_log.id`, nothing new introduced.
- `backend/src/app/flows/escalation_resolution_flow.py` — new. `EscalationResolutionFlow`, a second, decoupled CrewAI `Flow` (sad.md §2), 3 steps: `@start receive_resolution` (normalize `{original_inquiry_id, resolution_text}`) -> `@listen build_candidate_entry` (look up the original interaction via `get_interaction_by_id`; fail closed with `OriginalInquiryNotFound` if it doesn't exist — no orphaned candidate is ever written; shape a candidate KB entry mirroring ADR-005's `knowledge_base[]` fields: `candidate_intent` inherited from the original interaction's classified `intent` when available, `candidate_section="operator_resolution"`, `candidate_keywords=[]` — left for the Phase-3 Reviewer to fill in, not invented here — `candidate_content=resolution_text`) -> `@listen write_to_review_queue` (persists via `record_review_queue_entry`, **never** touches `domain_config.json`/a live KB table — verified structurally: this module has no import of `app.domain.loader` at all). `run_escalation_resolution(original_inquiry_id, resolution_text)` is the public entry point, mirroring `run_inquiry()`'s naming convention.
  - **Deliberate deviation from `run_inquiry()`'s pattern**: no `ThreadPoolExecutor`/`max_execution_time` timeout wrapper. This flow has zero LLM-backed steps (pure local persistence + one lookup) — sad.md §7's ceiling exists specifically because the reasoning Crew's LLM calls can hang; nothing here can. Documented as a judgment call, not an oversight (see Assumptions).

#### 13.3 API endpoints (`backend/src/app/main.py`)

- **`POST /email`** — `{from, subject, body}` -> `{reply_body, escalated: bool}`. Triggers `InquiryFlow` with `channel=email`. **Mapping decision** (sad.md pins the request/response shape, not this mapping — a `@backend.eng` judgment call, per this run's instructions): `sender_id = from` (mirrors `session_id`'s role for `/chat` — the guest's identifying handle for the channel); `raw_text = "Subject: {subject}\n\n{body}"` — both fields are concatenated (not just `body` alone) because a short subject line ("Billing question", "Room service request") can itself carry classification-relevant signal and, per FR-011, must pass through `pii_guard` just as much as the body; a plain readable concatenation was chosen over a structured/templated format since nothing downstream (pii_guard, the reasoning Crew) treats `raw_text` as anything but plain text. `EmailRequest.from_` uses a Pydantic `Field(alias="from")` (`from` is a Python reserved word) with `populate_by_name=True` for direct-construction convenience in tests.
- **`POST /escalations/{id}/resolve`** — `{resolution_text}` -> `{status: "queued", review_queue_id: str}`. Triggers `EscalationResolutionFlow`. **Two judgment calls, both documented in code comments and here** (sad.md leaves both open — "Response shape isn't pinned by the SAD table exactly... your call, document the reasoning"): (1) the path `{id}` is taken to be the `interaction_log` row `id` of the escalated interaction — sad.md's indicative data model doesn't name a separate "escalation id" concept, and `InquiryFlow` already persists exactly one interaction-log row per inquiry keyed by that `id`, so reusing it avoids inventing a redundant identifier/table; (2) the response returns only `status` + `review_queue_id` (not the full candidate record) — enough to confirm the write succeeded and to let a caller reference the queued item, without exposing internal candidate-entry shape over an endpoint that isn't `GET /review-queue` (Phase 3's job). Returns `404 {"error_code": "original_inquiry_not_found", "message": ...}` (same `{error_code, message}` envelope convention as the existing 500 handler) when `{id}` doesn't match any interaction-log record — `app.flows.escalation_resolution_flow.OriginalInquiryNotFound` is caught in the route and re-raised as a new `EscalationNotFoundError`, handled by a new `@app.exception_handler`.
- `ChatProcessingError`'s generic-failure message text was reworded from "processing your message" to "processing your request" since it's now shared by three routes (`/chat`, `/email`, `/escalations/{id}/resolve`), not renamed (kept its Phase-1 class name to avoid an unrequested rename touching Phase-1 code beyond this run's scope) — a judgment call, see Assumptions.
- `main.py`'s module docstring updated to describe the Phase 2 additions and reiterate that `GET /review-queue` and the approve/reject routes remain out of scope.

#### 13.4 Tests added this run

- `backend/tests/unit/test_review_queue.py` — 5 tests, mirrors `test_interaction_log.py`'s style exactly (init/create-file, record+list round-trip, optional-fields-omitted, `INSERT OR IGNORE` dedup-on-duplicate-id, most-recent-first ordering).
- `backend/tests/unit/test_interaction_log.py` — extended with 2 tests for the new `get_interaction_by_id` (found / not-found cases).
- `backend/tests/integration/test_escalation_resolution_flow.py` — 4 tests exercising `EscalationResolutionFlow`/`run_escalation_resolution` directly. **Fully deterministic, no `ANTHROPIC_API_KEY`/skip-if-no-key needed** (unlike `test_inquiry_flow.py`) — this flow has no LLM-backed steps at all, so every test here always runs: happy-path write-to-review-queue-linked-to-original-query, a structural "never touches domain/KB machinery" sanity check, `OriginalInquiryNotFound` raised (and no row written) on an unknown id, and a direct step-by-step call mirroring `test_inquiry_flow.py`'s `test_pii_redact_is_fail_closed_on_pii_guard_failure` pattern of calling Flow methods without `kickoff()`.
- `backend/tests/integration/test_email_endpoint.py` — mirrors `test_chat_endpoint.py`: 3 deterministic 422-validation tests (missing `from`/`subject`/`body`) plus 2 live tests (skip-if-no-key, same pattern) — one asserting the `{reply_body, escalated}` shape, one confirming the `channel="email"` mapping is actually visible via `GET /interactions`.
- `backend/tests/integration/test_escalation_resolve_endpoint.py` — mirrors `test_chat_endpoint.py`'s structure but, like the flow tests above, is **fully deterministic** (no live LLM call on this path): seeds an escalated `interaction_log` row directly via `record_interaction`, then exercises the real endpoint — queued-status/review_queue_id happy path (verified against the actual `review_queue` row), 422 on missing `resolution_text`, and 404-with-envelope on an unknown `{id}` (asserting no row was written).

**Bug found and fixed during this run's own verification** (not shipped, caught by tests before commit-review): `EscalationResolutionFlow.write_to_review_queue`'s persisted `record` dict initially omitted `resolution_text` (only `candidate_content`, which duplicates the same string, was included) — `record_review_queue_entry` requires `record["resolution_text"]` (a `KeyError`, surfaced immediately by the new tests as `write_to_review_queue: 'resolution_text'`). Fixed by adding `"resolution_text": self.state["resolution"]["resolution_text"]` explicitly to the record dict before persisting. Caught before any live verification or commit — flagged here per aamad-core.md's reproducibility/provenance expectation to record what actually happened, not just the clean end state.

**Live verification performed** (real Anthropic API calls, `backend/.venv`, real `ANTHROPIC_API_KEY` from `backend/.env`, fresh `backend/data/app.db`, `uvicorn app.main:app --port 8030 --app-dir src`):

- `POST /email` with a real check-in/check-out question (`from: guest.livetest@example.com`) -> `200 {"reply_body": "Thanks for your patience. This is taking longer than expected, so I've flagged it for a team member to review personally.", "escalated": true}` — this hit the pre-existing §7 `max_execution_time` timeout-escalation path (same, already-documented latency/ceiling gap as Phase 1, not a Phase-2 defect). `GET /interactions` afterward confirmed the row with `channel: "email"`, `sender_id: "guest.livetest@example.com"`, `outcome: "escalated"` — proving the `channel=email` mapping reaches `InquiryFlow`/the interaction log for real.
- To also positively demonstrate a **non-timeout, genuinely sentiment-driven** escalation (not just the timeout path), `run_inquiry(..., timeout_seconds=30)` was called directly (same bypass technique as Phase 1's §12 live verification) with a clearly angry/frustrated message ("This is absolutely ridiculous! ... I am furious and want a manager to call me back immediately!") -> real composed escalation, `escalated: True`, `reason: ["high_frustration"]`, `inquiry_id: "907a930a-4895-40ea-b97b-c132c5a4d4dd"` — a real `escalation_gate` decision (sentiment_score >= 0.75), not a ceiling artifact.
- `POST /escalations/{id}/resolve` called twice against real interaction-log ids — once against the timeout-escalated `/email` row, once against the real sentiment-escalated row above — both returned `200 {"status": "queued", "review_queue_id": ...}`.
- **Verified directly via `sqlite3`** (no `GET /review-queue` endpoint exists yet to check via HTTP, per this run's scope): queried `backend/data/app.db`'s `review_queue` table directly — both rows present, `status: "pending"`, correctly linked (`original_inquiry_id` matching each source interaction's `id`), the second row showing `original_query_text` matching the real angry message verbatim and `candidate_intent: "general_complaints"` (correctly inherited from the real classification the reasoning Crew produced) — confirming FR-008/AC-010's "linked to the original query" end-to-end against real data, not a mock.
- `404` path verified live: `POST /escalations/does-not-exist-123/resolve` -> `404 {"error_code": "original_inquiry_not_found", "message": "No interaction_log record found for id='does-not-exist-123'"}`.
- `422` path verified live: `POST /email` missing `from` -> `422` (FastAPI/Pydantic validation, same as Phase 1's pattern).
- Test db (`backend/data/app.db`) removed after verification (gitignored, not committed — confirmed via `git status`, no output for `backend/data/`).

**Full `pytest` suite**: **56 passed** (was 37 baseline + 19 new tests this run: 5 + 2 + 4 + 5 + 3, see §13.4), run 3 times to check stability against this environment's already-documented live-test flakiness (§11/§12) — 2 of 3 runs clean at 56/0, 1 run showed a single transient failure consistent with the same already-documented cross-test straggler-thread contamination mechanism (§11's "cross-test contamination" explanation — a prior live test's un-killable background thread landing a row in a different, still-monkeypatched test's `tmp_path` db), re-run confirmed clean; **not** a regression introduced by this phase's code (the specific bug this run introduced and fixed itself — §13.2's `resolution_text` `KeyError` — was caught deterministically by the new tests every single run, with no flakiness, since `EscalationResolutionFlow`'s own tests need no live API key at all).

`ruff check src tests` — clean on every file touched/added this run; same 2 pre-existing E501 nits in untouched predecessor files (`task_outputs.py`, `domain/loader.py`) remain, unrelated. `mypy src` — `Success: no issues found in 18 source files` (was 16; +2 new modules: `review_queue.py`, `escalation_resolution_flow.py`).

### 14. Phase 3 of MVP Build Sequencing (2026-08-21) — live-KB migration, `GET /review-queue`, `POST /review-queue/{id}/approve`, `POST /review-queue/{id}/reject`

**Scope of this action**: `sad.md`'s "MVP Build Sequencing" Phase 3, the final backend phase — sad.md §2 "Third, fully separate write path" (the *only* path that can mutate the live KB, NFR-008): review-queue read + Reviewer approve/reject, wired to a genuinely mutable KB store so an approved entry is "retrievable by `knowledge_retriever` from that point on" (FR-014, AC-011) inside the same running process, no restart.

#### 14.1 The core design problem: introducing a live, mutable KB

Before this phase, `domain/loader.py::kb_search` (ADR-005) read `knowledge_base` entries directly off the process-wide, `lru_cache`d `DomainConfig` singleton (`domain_config.json`, loaded once at start). `sad.md` §4 Data Architecture requires KB content be "seeded from `domain_config.json`, then mutable only via approved review-queue writes" — there was no such mutable layer at all, so AC-011 was structurally unsatisfiable pre-Phase-3: nothing could ever make a new entry retrievable without editing `domain_config.json` and restarting the process.

**New module**: `backend/src/app/persistence/knowledge_base.py` — plain stdlib `sqlite3` (no ORM), mirroring `interaction_log.py`/`review_queue.py`'s style exactly: same `backend/data/app.db` file, same `INTERACTION_LOG_DB_PATH` env var for test isolation. One table:

```sql
CREATE TABLE IF NOT EXISTS knowledge_base (
    kb_entry_id  TEXT PRIMARY KEY,
    intent       TEXT NOT NULL,
    section      TEXT NOT NULL,
    keywords     TEXT NOT NULL DEFAULT '[]',
    content      TEXT NOT NULL
);
```

Columns match `domain/loader.py::KBEntry`'s shape (ADR-005's pinned `knowledge_base[]` entry shape) exactly. Primitives: `count_entries`, `seed_from_domain_config(entries, ...)` (`INSERT OR IGNORE` keyed on `kb_entry_id` — idempotent, safe to call repeatedly), `insert_kb_entry(entry, ...)` (the sole way the table grows beyond its seed — Reviewer-approval writes only), `list_kb_entries(intent=None, ...)`.

**Seeding trigger** (a judgment call, since `sad.md` doesn't pin one — documented per this phase's task instructions): `domain/loader.py::kb_search` calls `count_entries()` at the top of every invocation (one cheap `SELECT COUNT(*)`, no writes) and only calls `seed_from_domain_config(config.knowledge_base)` when the table is genuinely empty. This keeps `knowledge_base.py` itself free of any dependency on `domain/loader.py` (avoiding a circular import, since `loader.py` now imports from `knowledge_base.py`), is cheap on the common case (every call after the first, whether seeded originally or grown via approvals), and is self-healing/idempotent for test-isolated `tmp_path` dbs — each fresh test db seeds itself from the real `domain_config.json` on first `kb_search` call, exactly matching pre-Phase-3 behavior. An `lru_cache`-guarded singleton (mirroring `get_domain_config()`) was considered and rejected: it would not reseed a *different* db path within the same process, breaking `tests/integration/test_inquiry_flow.py`'s existing `INTERACTION_LOG_DB_PATH`-monkeypatch-per-test isolation pattern.

**Taxonomy is untouched** — still read directly off the static `DomainConfig` singleton, exactly as before. Only `knowledge_base` entries moved to the mutable store, per this phase's task boundary.

**`kb_search()` update** (`domain/loader.py`): the ADR-005 scoring/floor(0.20)/top-3 algorithm is byte-for-byte unchanged — only the entry data source moved, from `config.knowledge_base` to `list_kb_entries(intent=intent)`. `config` is still accepted (used to source the seed data when the table is empty) for signature stability. Verified this doesn't regress the four FR-013 scenario-category tests in `test_inquiry_flow.py` (see Verification below — all pass unchanged).

#### 14.2 Review-queue read + approve/reject endpoints (`app.main`)

- **`GET /review-queue`** — lists all queued entries (pending + approved + rejected), most recent first, via the already-existing `list_review_queue()`. `sad.md` doesn't specify a status filter for this endpoint and none was invented (per this phase's task instructions).
- **`POST /review-queue/{id}/approve`** — request body (`ApproveReviewQueueRequest`) lets the Reviewer optionally override `intent`/`section`/`keywords`/`content`; any field left unset falls back to the queued candidate's stored `candidate_*` value. This matters in practice, not just as a nice-to-have: `EscalationResolutionFlow` (Phase 2) always writes `candidate_keywords=[]` (its own docstring: "left for the Reviewer to fill in at approval time, not invented here") — and ADR-005's keyword-overlap scoring skips any entry with an empty `keywords` list outright, so an entry approved with no keyword override could never actually be retrieved. Generates a new `kb_entry_id` (`kb-approved-{uuid4 hex[:12]}`), writes to the live `knowledge_base` table via `insert_kb_entry`, marks the `review_queue` row `status='approved'`, returns the resulting KB entry (`KBEntryResponse`, mirrors `KBEntry`'s 5 fields).
- **`POST /review-queue/{id}/reject`** — no request body (a rejection carries no editable content); marks `status='rejected'`, no KB write.
- **404 handling**: both routes return `404 {"error_code": "review_queue_entry_not_found", ...}` when `{id}` doesn't match any `review_queue` row (`get_review_queue_entry`, a new read added to `review_queue.py` alongside `update_review_queue_status`).
- **Re-approve/re-reject idempotency — judgment call, documented**: chosen to be a hard `409 {"error_code": "review_queue_already_actioned", ...}`, not a silent idempotent no-op. Rationale: unlike most of this codebase's `INSERT OR IGNORE` idempotent-write conventions (interaction log, review-queue writes, KB seeding), *approve* has a real, one-shot side effect — a live-KB write — and NFR-008's "no path to the live KB except explicit human approval" reads more safely as "each approval decision happens exactly once" than as "approving the same candidate twice is harmless." A silent idempotent re-approve risks either a second, redundant KB entry (if re-approve minted a fresh `kb_entry_id` each time, which it would, since ids aren't derived from the queue row) or requires inventing a new dedup key not otherwise needed — the 409 avoids both and makes the already-actioned state visible to the caller instead. Re-reject after reject (or after approve) is 409 for the same consistency reason, even though it has no side effect to duplicate — keeping both routes' state-machine semantics symmetric was judged less surprising than making reject idempotent-safe while approve is not.
- **422 on an unfillable approval**: if, after applying overrides, the resulting `intent` or `content` would still be empty (e.g. the queued candidate's `candidate_intent` is `None` — which happens whenever the original escalation was a `max_execution_time` timeout, since that path never reaches the classifier — and the Reviewer didn't supply an override), the route returns `422 {"error_code": "invalid_kb_entry", ...}` rather than writing a KB entry with a missing required field. This is not a hypothetical — it was hit for real during this phase's own live verification (see below), where the first live escalation happened to be a timeout with `candidate_intent=null`.

`review_queue.py` gained two small additions for this phase: `get_review_queue_entry(id, ...)` (read-by-id) and `update_review_queue_status(id, status, ...)` — no schema change, same file/table as Phase 2.

#### 14.3 Live verification performed (real Anthropic API calls, `backend/.venv`, real `ANTHROPIC_API_KEY` from `backend/.env`, fresh `backend/data/app.db`, `uvicorn app.main:app --port 8040 --app-dir src`) — the concrete AC-011 proof

1. **Real escalation -> real resolution -> pending queue row.** `POST /chat` with a strongly-worded room-condition complaint ("mold in the bathroom...") -> `200 {"escalated": true}` (this one hit the already-documented §7 `max_execution_time` timeout path, not a sentiment escalation — `GET /interactions` showed `outcome: "escalated"`, `intent: null`, `diagnostic` citing the 10s ceiling). `POST /escalations/{id}/resolve` with a realistic resolution -> `200 {"status": "queued", "review_queue_id": "c8a67286-..."}`. `GET /review-queue` confirmed one row, `status: "pending"`, `candidate_intent: null` (correctly inherited as null from the timeout-escalated interaction, which never reached the classifier), `candidate_keywords: []` (Phase 2's always-empty default).
2. **Approve with real keyword overrides** (required here since `candidate_intent` was null and `candidate_keywords` was empty — a real test of the optional-edit path, not a formality): `POST /review-queue/c8a67286-.../approve` with `{"intent": "general_complaints", "section": "Mold / mildew in bathroom", "keywords": ["mold", "mildew", "bathroom mold"]}` -> `200` returning the new KB entry: `{"kb_entry_id": "kb-approved-d497d692a8f1", "intent": "general_complaints", "section": "Mold / mildew in bathroom", "keywords": ["mold","mildew","bathroom mold"], "content": "We found mold growth behind the bathroom tile grout. Guests reporting bathroom mold or mildew should be moved to a new room immediately at no charge, and maintenance should schedule a deep clean and grout inspection for the original room before it is returned to inventory."}`.
3. **Direct `kb_search()` proof (persistence-layer, no LLM involved)**: in a separate Python process (same `backend/data/app.db`), `kb_search(intent="general_complaints", query_text="I noticed mold in my bathroom, is that normal?")` -> `match_found: True`, `retrieved_snippets: [{"kb_entry_id": "kb-approved-d497d692a8f1", "relevance_score": 0.3333, ...}]` — the newly-approved entry, immediately retrievable, no restart.
4. **End-to-end proof through the real pipeline** (the concrete AC-011 proof this task calls for): `run_inquiry(channel="chat", raw_text="I noticed some mold in my bathroom, is that something you can help with?", sender_id="guest-live-verify-p3-2", timeout_seconds=30)` (30s bypass of the 10s wrapper only for this verification step, same technique as every prior phase's live checks) -> `{"escalated": False, "reply": "Thank you for letting us know about this — we're sorry to hear about the mold in your bathroom... we'll move you to a new room right away at no charge, and our maintenance team will schedule a deep clean and grout inspection...", "reason": None, "inquiry_id": "3b3951ce-..."}` — the composed reply directly restates the newly-approved entry's content (room move, no charge, deep clean/grout inspection), not a generic answer. `GET /interactions` confirmed the persisted row: `intent: "general_complaints"`, `confidence: 0.85`, `match_found: true`, `grounded: true`, `outcome: "responded"` — full proof `knowledge_retriever` surfaced the new entry and `response_composer` grounded on it, live, same process, no restart.
5. **Reject path + `GET /review-queue` status correctness.** A second real escalation (`run_inquiry(..., timeout_seconds=30)` with an angry front-desk-rudeness complaint) produced a genuine sentiment-driven escalation (`reason: ["high_frustration"]`, not a timeout — `confidence`/`sentiment_score` populated normally). Resolved via `POST /escalations/{id}/resolve` -> queued a second candidate. `POST /review-queue/{id}/reject` -> `200 {"id": "4605225d-...", "status": "rejected"}`. `GET /review-queue` afterward showed both rows with correct, distinct statuses (`"...cd59": "approved"`, `"...987": "rejected"`). Confirmed directly via `list_kb_entries()` that the live KB has exactly 13 rows (12 original seed + 1 approved) — the rejected candidate never touched the KB.

Verification (repo state): the live-verification `backend/data/app.db` was removed after this section's checks (gitignored, confirmed via `git status` — no output for `backend/data/`), same convention as every prior phase's live verification.

#### 14.4 Tests added this phase

- `backend/tests/unit/test_knowledge_base.py` — 8 tests: `init_db`/file creation, `count_entries` on a fresh table, `seed_from_domain_config` populates + is idempotent (a second seed call with different `content` for the same `kb_entry_id` does not overwrite), `insert_kb_entry` adds a row, `list_kb_entries` filters by intent, and — the two tests that most directly exercise this phase's design — `kb_search` finds a manually-inserted live entry (standing in for a Reviewer approval, isolated from the endpoint layer) and `kb_search` still self-seeds and finds the real `domain_config.json` entries on a completely fresh table (the regression guard for `test_inquiry_flow.py`'s scenario-category tests).
- `backend/tests/integration/test_review_queue_endpoints.py` — 11 tests covering `GET /review-queue` (empty list, most-recent-first ordering), `POST .../approve` (writes a live KB entry + marks `status='approved'`, the optional-edit override path overriding all 4 fields, 404 on unknown id, 409 on re-approving an already-approved row, 422 when `intent` is missing with no override supplied), `POST .../reject` (marks `status='rejected'` with zero KB writes, 404, 409 on re-rejecting), and a final test confirming `GET /review-queue` shows correct, distinct statuses for a mix of approved/rejected entries.

**Full `pytest` suite**: **75 passed** (was 56 baseline + 19 new tests: 8 + 11), run 3 times for stability (all 3 runs clean, 75/0 — no repeat of the transient cross-test straggler-thread flakiness noted in Phase 2's entry, though that mechanism remains a known, unrelated pre-existing characteristic of this environment, not something this phase's tests are immune to by design). Some post-session stderr noise (`RuntimeError: cannot schedule new futures after shutdown`, `CrewAIEventsBus` pairing-mismatch warnings) appears *after* pytest's own `"75 passed"` summary line in every run — confirmed this is the same already-documented un-killable-background-thread characteristic (backend.md §5/§11: a straggler `InquiryFlow` thread from a timeout-path test keeps running past its own test's completion and even past the full session's end) surfacing as executor-shutdown noise once the test process itself starts tearing down; it is not a test failure and does not affect the reported pass count in any of the 3 runs.

`ruff check src tests` — clean on every file touched/added this phase (`knowledge_base.py`, `domain/loader.py`, `review_queue.py`, `main.py`, both new test files); the same 1 pre-existing E501 nit in `task_outputs.py` (untouched this phase) remains — the previously-flagged 2nd pre-existing nit, in `domain/loader.py`, no longer applies: this phase legitimately modified that file (the `kb_search` signature wrap needed for the new import lines pushed it over 100 chars at a different line number, fixed inline rather than left as a nit, since the file is no longer "untouched predecessor code" this phase). `mypy src` — `Success: no issues found in 19 source files` (was 18; +1 new module, `knowledge_base.py`).

## Sources

- `project-context/1.define/prd.md`
- `project-context/1.define/sad.md` (§1 ADR-001-005, §2 Multi-Agent System Specification / Task-Turn Orchestration / Typed Task Outputs, §4 Backend Architecture, §7 Performance & Scalability, §9 Testing & QA, MVP Build Sequencing)
- `project-context/2.build/setup.md` (scaffold state, Open Question re: backend venv split — resolved this run, §0 above)
- `.claude/rules/aamad-core.md` (Deterministic execution, fail-closed on ambiguity, Sources/Assumptions/Open Questions/Audit contract)
- `.claude/rules/adapter-crewai.md` (YAML-externalization, `Process.sequential`, `max_iter<=12`, `max_retry_limit>=2`, `allow_delegation=false`, temperature/Quality Gate guidance)
- `backend/src/app/schemas/task_outputs.py`, `backend/src/app/domain/loader.py` — predecessor's completed work, read in full, not modified
- `backend/domain_config.json`, `backend/domain_config.schema.json` — used as-is (ADR-005 seed data)
- Installed package versions discovered this run: `crewai==1.15.17`, Python 3.11.16 (`backend/.venv`)
- Two design decisions confirmed with the user before this run started (not re-litigated): (1) PII detection is a deterministic regex/pattern-based tool, not LLM-only; (2) this run stops at a working `InquiryFlow.kickoff()`/`run_inquiry()` callable from Python/pytest — no FastAPI route wiring, no `main.py` changes
- **Phase 2 run (2026-08-21)**: `project-context/1.define/sad.md` (§2 "Decoupled second flow, `EscalationResolutionFlow`", §4 `POST /email`/`POST /escalations/{id}/resolve` API rows, "MVP Build Sequencing" Phase 2 row), read in full per this run's instructions before starting; `backend/src/app/flows/inquiry_flow.py` (read in full to confirm `intake_normalize` is already channel-agnostic — §13.1); `backend/src/app/persistence/interaction_log.py` (read for the exact `id` column name before adding `get_interaction_by_id`)
- **Phase 3 run (2026-08-21)**: `project-context/1.define/sad.md` (§2 "Third, fully separate write path", §4 Data Architecture "seeded from domain_config.json, then mutable only via approved review-queue writes" / `GET /review-queue`, `POST /review-queue/{id}/approve`, `POST /review-queue/{id}/reject` API rows, ADR-005, "MVP Build Sequencing" Phase 3 row), read in full per this run's instructions before starting; `backend/src/app/domain/loader.py`, `backend/src/app/persistence/review_queue.py`, `backend/src/app/flows/escalation_resolution_flow.py` — all read in full before extending/modifying, per this run's explicit instruction (including `escalation_resolution_flow.py`'s docstring noting `candidate_keywords` is always written as `[]`, which directly shaped the approve endpoint's optional-edit request design, §14.2)

## Assumptions

- **Backend virtual environment split** (resolves a setup.md Open Question): `backend/.venv` (Python 3.11.16) is now the backend's own environment, separate from the shared root `.venv` (Python 3.14.5, incompatible with crewai). Anyone running backend tests/lint locally should activate `backend/.venv`, not the root one. `setup.md`'s §5 "how to install and run locally" instructions (which assume the shared root `.venv`) are now stale for the backend half and should be updated by whoever next touches `setup.md` — flagged here rather than silently edited, since `setup.md` is `@project.mgr`'s artifact, not this persona's.
- **Model provider-prefix convention**: `sad.md`/ADR-004 name bare model tiers (`claude-haiku-4-5`, `claude-sonnet-5`); this run prefixes them `anthropic/...` in `agents.yaml`, the convention CrewAI's LiteLLM-based `LLM` routing expects. Not a `sad.md`-specified detail — an implementation necessity.
- **`temperature: 0.2`** for all 5 agents is a `@backend.eng` judgment call (§7 above), not a `sad.md`-pinned value. `sad.md` is silent on temperature. Flagged for `@qa.eng`/stakeholder review alongside the escalation thresholds, which `sad.md` already documents as "MVP starting values ... not a final calibration."
- **`pii_guard` production path is a real LLM-backed CrewAI agent call** (bound to the deterministic `pii_detector` tool), not a bypass-the-LLM direct function call — this follows ADR-004's rationale (`pii_guard` is explicitly costed/tiered as a per-inquiry Haiku call: "cost and latency compound most there ... well within Haiku's capability") and the stakeholder-confirmed decision's phrasing ("a tool that pii_guard uses"). The tool guarantees deterministic *detection*; the agent's role is to invoke it and relay its output faithfully (enforced via task/backstory instructions, not code-level cross-validation against the tool's raw output). This is a considered interpretation, not a certainty — recorded as an Open Question below for confirmation, since a stricter reading of "fail-closed" could argue for bypassing the LLM entirely on this security-critical path.
- **`max_execution_time` enforcement lives in `run_inquiry()`, not as a CrewAI `Flow` constructor field** — §5 above explains why (no native flow-level cap in the installed crewai version). Functionally equivalent to `sad.md` §7's requirement, but implemented as an external wrapper rather than a first-class Flow setting.
- **`DATABASE_URL` env var (`.env.example`) is not read directly** by `interaction_log.py` — it resolves the DB path from an explicit function argument, then `INTERACTION_LOG_DB_PATH` (a new env var this run introduces, used for test isolation), then a hardcoded default (`backend/data/app.db`) that matches `DATABASE_URL`'s value in spirit but isn't parsed from it. Low-risk for MVP (single fixed path), flagged for whoever wires real config-file/env precedence later.
- **Ruff E501 nits in predecessor files** (`task_outputs.py:44`, `domain/loader.py:97`, both 1-2 chars over the 100-column limit) were left as-is — those files were confirmed "done and correct" and explicitly not to be diverged from in this run's instructions; fixing a cosmetic lint nit there felt like exactly the kind of unrequested edit that risks diverging from a reviewed baseline.
- `backend/pyproject.toml` dependency `crewai>=0.80` was updated to `crewai[anthropic]>=1.15` to match what is actually installable/importable today (see §0); the original unpinned lower bound from scaffold time could not have resolved crewai's real Anthropic-provider requirements.
- **`*implement-endpoint`**: `POST /chat` is registered directly in `main.py` rather than a separate router module — a single route didn't justify the extra indirection; if `POST /email` lands in Phase 2 alongside it, splitting into `api/chat.py`/`api/email.py` router modules at that point would be reasonable, not done preemptively here. `channel="chat"` is hardcoded in the route handler (not taken from the request body) since sad.md §4 defines `POST /chat` as the chat-specific endpoint — `channel` is an internal `InquiryFlow` concept, not part of the public request schema.
- **Phase 2, `POST /email` mapping** (§13.3): `sender_id = from`, `raw_text = "Subject: {subject}\n\n{body}"` — sad.md pins `POST /email`'s request/response shape but not this mapping; a `@backend.eng` judgment call, made per this run's explicit instruction to decide and document. Alternatives considered and rejected: `raw_text = body` alone (discards subject-line signal that could matter for classification/PII-scan, and FR-011 requires PII redaction on "any content", which arguably includes the subject line); a structured/multi-field prompt template (unnecessary — nothing downstream treats `raw_text` as anything but plain text, `POST /chat`'s `message` is likewise unstructured).
- **Phase 2, `POST /email` did not split `main.py` into router modules** despite the prior Phase-1 Assumption ("if `POST /email` lands in Phase 2 alongside it, splitting ... would be reasonable") flagging that as a likely next step. Decided against it this run: `main.py` is still a single, linearly-readable file (7 routes total after this phase) and splitting would be a structural refactor beyond this run's stated scope (`*implement-endpoint`/`*develop-be` for the specific Phase-2 endpoints, not a reorganization of Phase-1 code) — flagged here rather than silently deferred again, since the prior Assumption explicitly anticipated this decision point.
- **Phase 2, `POST /escalations/{id}/resolve`'s `{id}` = `interaction_log.id`**: sad.md's data model has no separate "escalation" entity/id — every inquiry (escalated or not) gets exactly one `interaction_log` row keyed by `id` (per the Phase-1 double-log fix, §11). Reusing that same id as the path parameter avoids inventing a redundant "escalation record" concept/table this phase doesn't otherwise need; the endpoint validates the id exists (404 if not) but does **not** additionally validate `outcome == "escalated"` — a resolution submitted against a `"responded"` or `"diagnostic_halt"` row is still accepted and queued. This was a deliberate simplicity choice (the SAD doesn't require rejecting that case, and a real operator UI would only ever surface escalated items as resolvable in the first place, per sad.md §3's ops-view description) rather than an oversight — flagged as an Open Question below in case stricter validation is wanted later.
- **Phase 2, `review_queue` reuses the `INTERACTION_LOG_DB_PATH` env var** rather than introducing a new one (e.g. `REVIEW_QUEUE_DB_PATH`) — both tables always live in the same `backend/data/app.db` file (sad.md §4: "one local SQLite store"), so one path-resolution knob keeps test isolation consistent between `interaction_log.py` and `review_queue.py` (a test that monkeypatches one env var isolates both tables at once, exactly as this run's own tests rely on).
- **Phase 2, `EscalationResolutionFlow` has no `max_execution_time`/timeout wrapper** (unlike `run_inquiry()`) — a deliberate scope-narrowing, not an oversight: this flow has zero LLM-backed steps (only a local db read + a local db write), so sad.md §7's ceiling (which exists specifically to bound a hanging LLM call) has nothing to bound here. If a future phase adds an LLM-backed step to this flow (e.g. auto-drafting `candidate_keywords`), this assumption would need revisiting.
- **Phase 2, `ChatProcessingError`'s error message reworded** ("processing your message" -> "processing your request") since it's now the shared unexpected-failure handler for `/chat`, `/email`, and `/escalations/{id}/resolve` — the class itself was **not** renamed (kept `ChatProcessingError`, its Phase-1 name) to avoid an unrequested rename touching reviewed Phase-1 code beyond what this run needed to change; a future run could rename it to something channel-neutral (e.g. `ProcessingError`) if that naming debt is worth paying down deliberately.
- **Phase 3, live-KB seeding trigger** (`domain/loader.py::kb_search` checks `count_entries() == 0` on every call and seeds from `config.knowledge_base` only when empty — see backend.md §14.1 for the full write-up): chosen over (a) an `lru_cache`-guarded singleton mirroring `get_domain_config()`, rejected because it would not reseed a *different* db path within the same process and would break the `INTERACTION_LOG_DB_PATH`-per-test isolation `test_inquiry_flow.py`/`test_knowledge_base.py` rely on, and (b) unconditional reseeding on every call, rejected as a pointless write-attempt cost on the hot retrieval path once already seeded. `knowledge_base.py` itself has no dependency on `domain/loader.py` (the seed *data* is always passed in by the caller) — a deliberate layering choice to avoid a circular import, since `domain/loader.py` imports from `knowledge_base.py`.
- **Phase 3, re-approve/re-reject idempotency is a hard `409`, not a silent no-op** — a deliberate departure from this codebase's usual `INSERT OR IGNORE` idempotent-write convention (interaction log, review-queue writes, KB seeding all use it), because *approve* has a one-shot side effect (a live-KB write, always minting a fresh `kb_entry_id`) that a silent re-approve would either duplicate or require a new dedup key to prevent. Reject was made symmetric (also `409` on repeat) purely for consistent state-machine semantics across both routes, even though reject itself has no side effect to duplicate.
- **Phase 3, `POST /review-queue/{id}/approve`'s optional-edit request shape** (`intent`/`section`/`keywords`/`content`, all optional, falling back to the queued candidate's stored `candidate_*` values) — sad.md's "Approve (optionally edited)" line doesn't pin an exact request shape. All 4 candidate-shape fields (ADR-005's `knowledge_base[]` fields minus `kb_entry_id`, always freshly generated) were made independently overridable rather than requiring the whole entry be resupplied — `candidate_keywords` in particular is *always* empty per Phase 2's `EscalationResolutionFlow` docstring and *always* needs Reviewer input to be retrievable at all (ADR-005 skips any entry with 0 keywords outright), which is why this phase's own live verification (§14.3) deliberately exercised the keywords override, not just the happy path.
- **Phase 3, generated `kb_entry_id` uses the prefix `kb-approved-{uuid4 hex[:12]}`** rather than a bare UUID — a minor, non-load-bearing stylistic choice distinguishing Reviewer-approved entries from the seed data's `kb-{category}-NNN` convention at a glance in `GET /review-queue`-adjacent tooling/log output. Not specified by sad.md.

## Open Questions

Carried from `sad.md` (unresolved, not this persona's to resolve): actual project budget; which PII regulation (if any) must ultimately be complied with; MVP hosting/infrastructure target (`@devops.eng`, Phase 3).

New, raised by this run:

- **No `ANTHROPIC_API_KEY` was available in this environment** (no `backend/.env`, no shell env var) — the 4 true end-to-end `InquiryFlow` scenario-category tests in `tests/integration/test_inquiry_flow.py` are written and parametrized per FR-013 category but were **skipped**, not run, this session. Whoever has a real key should run `pytest -m integration` (or just `pytest`) with `ANTHROPIC_API_KEY` set to actually exercise the reasoning Crew and `pii_guard` against live Claude models before treating Phase 1 as fully verified end-to-end. This also means the §7 Latency Spike (mandatory gate before Phase 2 per `sad.md`) has **not** been run yet — it requires 20+ real requests across all four scenario categories, which this run's environment cannot produce.
- **Is `pii_guard`'s production path correctly interpreted as "real LLM call, tool-grounded" rather than "bypass the LLM, call the tool directly"?** See Assumptions above — this is the single most safety-critical step (FR-011, Must-priority, fail-closed) and the interpretation was made without stakeholder re-confirmation (the original two decisions didn't spell out this specific sub-question). If the stakeholder's intent was closer to "never let an LLM anywhere near the actual redaction decision," the fix is small (call `detect_pii()` directly inside `InquiryFlow.pii_redact` instead of going through `kickoff_pii_guard()`) but changes the cost/latency profile ADR-004 assumed.
- **`setup.md`'s local-run instructions are now partially stale** (§0/Assumptions above) — they assume one shared root `.venv`; the backend now needs its own. Not fixed here since `setup.md` belongs to `@project.mgr`.
- **`max_execution_time`'s un-killable background thread on timeout** (§5) — acceptable for MVP's no-real-traffic scope per `sad.md` §7, but would need a real cancellation strategy (e.g. cooperative cancellation tokens, or a process-per-request model) before any concurrent/production use.
- Whether the `0.2` temperature choice (Assumptions) should instead be pinned explicitly in `sad.md` alongside the escalation thresholds, given both are described there as "MVP starting values ... expected to be tuned from `@qa.eng` acceptance results."

New, raised by `*implement-endpoint`:

- ~~`temperature: 0.2` on the Haiku-tier agents (`query_classifier`, `knowledge_retriever`) intermittently causes live Anthropic API 400s...~~ **RESOLVED 2026-08-20** — see "10. `*implement-endpoint` follow-up fix" above. Root cause was misattributed to the Haiku-tier agents; direct reproduction shows `claude-sonnet-5` (not `claude-haiku-4-5`) is the model that rejects `temperature`, 100% reproducible. Fixed in `agents.yaml` by omitting `temperature` only on the 2 Sonnet-tier agents (`sentiment_analyzer`, `response_composer`); the 3 Haiku-tier agents (`query_classifier`, `knowledge_retriever`, `pii_guard`) keep `temperature: 0.2` unchanged. Live-verified end-to-end (4 scenario-category `/chat` requests + 2 direct `InquiryFlow.kickoff()` runs), zero `temperature`-related errors.
- The §7 Latency Spike (mandatory gate before Phase 2 per sad.md) still has not been run as a proper 20+-request sample — this run's live verification was a handful of ad hoc requests (enough to prove the endpoint wiring and error handling, not enough to satisfy the spike's sample-size requirement). The temperature bug that previously would have contaminated the spike's measurements is now resolved; however, see the new `run_inquiry()` double-log/timeout finding immediately below — that should also be resolved (or at least accounted for) before the spike is run, since real full-pipeline latency (~15-16s observed) now reliably exceeds the current 10s ceiling.
New, raised by `*implement-endpoint` follow-up (`GET /interactions`):

- **`sad.md`'s MVP Build Sequencing table still shows `GET /interactions` as Phase 3** — this file's §12 documents the operator's explicit decision to pull it forward now; `sad.md` itself was not edited (not this persona's artifact to change) and still reads as Phase 3 for that endpoint. Flagged so a future reader of `sad.md` alone (without this file) isn't misled — cross-reference backend.md §12 for the actual current state.
- **The live verification above reproduced the sad.md §7 timeout-escalation path on the very first live `/chat` call** (real flow latency still exceeds the 10s ceiling, consistent with §11's already-flagged, still-open finding) — this is expected/known behavior, not a new defect, but it does mean `GET /interactions` rows will commonly show `outcome: "escalated"` with mostly-`null` fields (timeout diagnostic) rather than a fully-populated "responded" row, until the ceiling/latency mismatch flagged in §11's Open Questions is actually resolved. Not in this action's scope to fix.

- ~~**NEW (2026-08-20), not fixed — `run_inquiry()`'s 10s timeout wrapper does not actually return early and double-logs on timeout.**~~ **RESOLVED 2026-08-20** — see "11. `run_inquiry()` double-log fix" above. `run_inquiry()` no longer holds the `with` block open past `future.result()` (manual `pool.shutdown(wait=False)` in a `finally`), and a shared `inquiry_id` passed through `kickoff(inputs=...)` plus `interaction_log.py`'s `INSERT OR IGNORE` (keyed on the existing `id` PRIMARY KEY, no schema change) guarantees at most one persisted row per inquiry even when the un-killable background thread eventually reaches its own `log_interaction` step. Live-verified: `record_interaction` observed being called twice with the same `inquiry_id` (timeout path + straggler), only one row persisted; full `pytest` suite now 35 passed / 0 failed. The flow's real observed latency (~15-16s) still exceeds the sad.md §7 10s ceiling — that gap itself is unchanged/not in this fix's scope (the ceiling value and escalation behavior were explicitly not to be altered) — but `run_inquiry()` now degrades to the escalate branch and returns within a few seconds of the ceiling instead of blocking for the full real duration, matching sad.md §7's stated intent. Still open: whether the ceiling itself (10s) or the reasoning-Crew's real latency should be tuned so timeouts aren't the *common* case in practice — flagged for `@qa.eng`/stakeholder review alongside the Latency Spike, not resolved by this fix.

New, raised by Phase 2 (`channel=email`, `EscalationResolutionFlow`, review queue, `POST /email`/`POST /escalations/{id}/resolve`, 2026-08-21):

- **The mandatory §7 Latency Spike gate between Phase 1 and Phase 2 has still not been run** (carried forward, not newly discovered — flagged at the end of every Phase-1 entry above, and again here per this run's explicit instruction to "note it again briefly ... if not already noted"). This run proceeded into Phase 2 on an explicit operator decision to skip that gate for now, not because the gap was resolved. Its already-known consequence reproduced again during this run's own live verification: the real reasoning-Crew pipeline still commonly exceeds the 10s ceiling, so a meaningful fraction of `POST /email`/`POST /chat` responses will be timeout-driven escalations rather than genuine `escalation_gate` decisions in this environment, until the spike is run and the fallback ladder (sad.md §7) is applied if needed.
- **`POST /escalations/{id}/resolve` does not validate `outcome == "escalated"`** on the target interaction — accepts a resolution against any existing `interaction_log` row regardless of its outcome (see Assumptions). Worth a stakeholder/`@qa.eng` call on whether that should be a hard 4xx rejection (e.g. `409`) instead, once a real `/ops` UI (Phase 3) only ever offers this action on genuinely escalated items and the question of programmatic misuse becomes concrete.
- **`candidate_keywords` is always written as `[]`** by `EscalationResolutionFlow` — no keyword extraction/inference from `resolution_text` or the original query was attempted this phase (deliberately, per this run's scope — "This flow ONLY writes the candidate entry to the review queue"). Phase 3's Reviewer approve/edit flow will need some way to populate these before a candidate can usefully match ADR-005's `kb_search` once approved — either manual Reviewer entry (simplest, no new logic) or a future keyword-extraction step; not decided here, flagged for whoever scopes Phase 3.
- **`candidate_section`is hardcoded to the literal string `"operator_resolution"`** for every candidate entry (rather than being derived from the original interaction's `intent`/context) — a placeholder value chosen because ADR-005's `knowledge_base[]` schema requires a `section` field but sad.md doesn't define what "section" should mean for an operator-resolution-derived candidate specifically (existing seed data's `section` values describe *content structure* within an intent, e.g. by topic sub-area — a resolution transcript doesn't naturally have that same shape). Reviewer can edit this in Phase 3; not treated as authoritative KB metadata until then.

New, raised by Phase 3 (live-KB migration, `GET /review-queue`, `POST /review-queue/{id}/approve`/`.../reject`, 2026-08-21):

- **The §7 Latency Spike is now unblocked but still not run.** This was Phase 3, the final backend build phase — all three `MVP Build Sequencing` phases are now code-complete, so nothing about backend scope itself still blocks the spike; it remains genuinely not run as a proper 20+-request sample, carried forward one more time. `@qa.eng`/`@backend.eng` (or whoever runs Deliver-phase QA) should treat this as the top follow-up before any latency-sensitive sign-off.
- **`POST /review-queue/{id}/approve` accepts a candidate whose `candidate_intent` doesn't match any real `taxonomy[].intent` value** (e.g. a Reviewer typo, or an override that doesn't correspond to a seeded/known intent) — the route validates only that `intent`/`content` are non-empty (422 otherwise), not that `intent` is a recognized value from `domain_config.json`'s taxonomy. A KB entry with an unrecognized intent would simply never be retrieved (ADR-005's filter step requires `entry.intent == intent`, and `intent` always comes from the classifier, which only ever emits taxonomy-defined values) — a silent dead entry, not a crash or data-integrity issue, but worth a stricter validation pass if a real `/ops` UI (Integration phase) doesn't already constrain the Reviewer's `intent` field to a dropdown of known values.
- **No keyword-quality/uniqueness validation on approve** — the Reviewer can approve an entry with keywords that never clear ADR-005's 0.20 floor against realistic queries (e.g. keywords that don't actually appear in guest phrasing), silently producing an unretrievable entry with no error or warning. This mirrors the same class of gap as the `intent`-validation question above; not treated as a Phase 3 blocker since sad.md doesn't require it, but flagged for whoever scopes a real Reviewer UI.
- **Taxonomy remains fully static (no live/mutable store, no write path)** — confirmed as explicitly in-scope-to-leave-alone per this phase's task boundary, not an oversight. If a future phase needs Reviewer-driven taxonomy changes (e.g. a brand-new intent category), that would need its own design pass; nothing in this phase's `knowledge_base.py` generalizes to it.

## Audit

- **Timestamp**: 2026-08-19
- **Persona**: `@backend.eng`
- **Action**: `*develop-be` (continuation of a prior killed-mid-way run; predecessor's `schemas/task_outputs.py` and `domain/loader.py` verified correct and reused unmodified)
- **Resolved runtime**: `crewai` (`AAMAD_TARGET_RUNTIME=crewai`, consistent with `aamad.config.yml` `runtime.target: crewai` — no conflict)
- **Tools/versions used**: Python 3.11.16 (`backend/.venv`, split from the incompatible-with-crewai root `.venv` Python 3.14.5 — see §0), `crewai==1.15.17` (+ `anthropic` extra), `pytest==9.1.1`, `ruff==0.16.3`, `mypy==2.3.1`
- **Models / temperature / token controls** (adapter-claude... n/a, this is the crewai adapter — see adapter-crewai.md Logging rule): see §7 table above — `claude-haiku-4-5` (query_classifier, knowledge_retriever, pii_guard) and `claude-sonnet-5` (sentiment_analyzer, response_composer) via `anthropic/...` LiteLLM routing strings, `temperature=0.2` all agents, `max_iter<=12`, `max_retry_limit>=2`, `allow_delegation=false`
- **Design decisions honored from prior user confirmation (not re-litigated this run)**: (1) PII detection implemented as a deterministic regex/pattern-based tool (`tools/pii_detector.py`), not LLM-only — see §3; (2) this run stops at `InquiryFlow.kickoff()`/`run_inquiry()` callable from Python/pytest — `backend/src/app/main.py` and all FastAPI route logic were **not** touched, confirmed via `git status` showing no diff to `main.py`
- **Prohibited actions confirmed avoided**: no `EscalationResolutionFlow`, no Reviewer KB-write path, no FastAPI route registration, no `escalation_manager`/`interaction_logger` added as CrewAI agents (ADR-002 upheld — both remain plain Flow/pure-function code)
- **Verification performed**: `pytest` (28 passed, 4 skipped — see §8), `ruff check .` (2 pre-existing nits in untouched predecessor files only), `mypy src` (clean, 16 files)

- **Timestamp**: 2026-08-20
- **Persona**: `@backend.eng`
- **Action**: `*implement-endpoint` (scoped to `POST /chat` only, per sad.md §4 and this run's boundary — no `/email`, no escalation-resolution/review-queue endpoints)
- **Resolved runtime**: `crewai` (`AAMAD_TARGET_RUNTIME=crewai`, consistent with `aamad.config.yml` `runtime.target: crewai` — no conflict)
- **Tools/versions used**: Python 3.11.16 (`backend/.venv`), `crewai==1.15.17`, `fastapi>=0.115`/`pydantic>=2.7` (unchanged), `python-dotenv>=1.0` (now actually wired via `load_dotenv()` in `main.py`), `pytest==9.1.1`, `ruff==0.16.3`, `mypy==2.3.1`, `uvicorn` (manual live server verification)
- **Files changed**: `backend/src/app/main.py` (added `POST /chat`, `ChatRequest`/`ChatResponse`, `ChatProcessingError` + exception handler, `load_dotenv()`); `backend/tests/integration/test_chat_endpoint.py` (new)
- **Verification performed**: `ruff check` (clean), `mypy src` (clean, 16 files, unchanged from §8), `pytest tests/integration/test_chat_endpoint.py` (3 passed, including 1 live end-to-end call), full `pytest` suite (33 passed on a clean run; the 4 live scenario tests in `test_inquiry_flow.py` plus the 1 live test here are intermittently affected by the temperature finding below, not by anything in this action's own code), live `uvicorn` server hit with real `curl` requests (`GET /health` 200, `POST /chat` missing-field 422, `POST /chat` real message → both a normal 200 reply and — reproducing the temperature finding — a clean 500 `{error_code, message}` envelope with no leaked stack trace, confirmed against the server-side log)
- **Prohibited actions confirmed avoided**: no `POST /email`, no `POST /escalations/{id}/resolve`, no review-queue endpoints, no auth, no rate limiting, no streaming — `main.py`'s diff is limited to the single `POST /chat` route and its supporting models/handler
- **Known gap flagged, not fixed this run**: intermittent `temperature`-deprecated 400s from Anthropic on the Haiku-tier agents (see §9 Finding, Open Questions) — pre-existing `agents.yaml` config from `*develop-be`, out of this action's scope

- **Timestamp**: 2026-08-20
- **Persona**: `@backend.eng`
- **Action**: `*implement-endpoint` follow-up fix — corrected `temperature`-deprecated 400 bug found during live verification (blocking the SAD §7 Latency Spike), per this run's explicit bug-fix-only scope (no new features, no Phase 2/3 scope creep)
- **Resolved runtime**: `crewai` (`AAMAD_TARGET_RUNTIME=crewai`, consistent with `aamad.config.yml` `runtime.target: crewai` — no conflict)
- **Tools/versions used**: Python 3.11.16 (`backend/.venv`), `crewai==1.15.17` (native Anthropic provider, no LiteLLM installed — confirmed), `anthropic==0.73.0`, `pytest==9.1.1`, `uvicorn` (manual live server verification on port 8010, port 8000 already occupied by a stale process)
- **Root cause**: `claude-sonnet-5` (not `claude-haiku-4-5`, correcting the `*implement-endpoint` action's original misattribution) rejects the `temperature` request parameter on the live Anthropic API — `anthropic.BadRequestError: 'temperature' is deprecated for this model`, reproduced 3/3 via a direct, isolated `anthropic` SDK probe (bypassing CrewAI). `claude-haiku-4-5` accepts `temperature` normally, 3/3. Confirmed to be a live Anthropic API-side behavior, not a CrewAI/LiteLLM client bug — CrewAI's native Anthropic completion path (`crewai/llms/providers/anthropic/completion.py`) forwards `temperature` unconditionally with no model-aware guard, and no LiteLLM package is even installed in this project's dependency set.
- **Fix applied**: `backend/src/app/config/agents.yaml` — `temperature: 0.2` kept only on the 3 `claude-haiku-4-5` agents (`query_classifier`, `knowledge_retriever`, `pii_guard`); omitted (not `null`, simply absent) on the 2 `claude-sonnet-5` agents (`sentiment_analyzer`, `response_composer`). File header comment block rewritten to state the corrected per-model reasoning; stale "temperature: 0.2 for all 5 agents" claim removed. No code changes required (`agents/config_loader.py::build_agent` already handled an omitted `temperature` key correctly).
- **Verification performed**: direct live Anthropic API probe (3 reps/model, both with and without `temperature`); live `uvicorn` server (port 8010) hit with 4 real `POST /chat` requests across all 4 `domain_config.json` scenario categories, all 200 with zero `temperature` errors; 2 direct `InquiryFlow().kickoff()` runs (bypassing the 10s wrapper) confirming all 5 agents complete successfully end-to-end (14.98s and 16.05s respectively, zero errors in captured stderr); `pytest tests/integration/test_chat_endpoint.py` (3/3 passed); full `pytest` suite (31 passed, 4 failed — the 4 failures are an unrelated, separately-diagnosed pre-existing `run_inquiry()` timeout/double-log defect, not a temperature regression; see "10. `*implement-endpoint` follow-up fix" and Open Questions)
- **Prohibited actions confirmed avoided**: no changes to `main.py`, `inquiry_flow.py`, `config_loader.py`, or any file besides `agents.yaml` and this backend.md log; the newly-found `run_inquiry()` timeout/double-log defect was documented, not fixed (out of this action's declared scope)
- **Known gap flagged, not fixed this run**: `run_inquiry()`'s `with ThreadPoolExecutor(...) as pool:` block blocks on `shutdown(wait=True)` past `future.result()`'s timeout, causing (a) the function to not actually return early on timeout and (b) a duplicate interaction-log row once real flow latency (~15-16s) exceeds the 10s ceiling — see "10. ... New finding, not fixed" and Open Questions above

- **Timestamp**: 2026-08-20
- **Persona**: `@backend.eng`
- **Action**: `*implement-endpoint` follow-up fix #2 — fixed the `run_inquiry()` blocking-`shutdown`/duplicate-interaction-log defect flagged (not fixed) by the previous audit entry, per this run's explicit narrow scope (blocking-shutdown + double-log only; no change to the 10s SAD §7 ceiling value or timeout-escalation behavior; no `POST /chat`/`agents.yaml` changes)
- **Resolved runtime**: `crewai` (`AAMAD_TARGET_RUNTIME=crewai`, consistent with `aamad.config.yml` `runtime.target: crewai` — no conflict)
- **Tools/versions used**: Python 3.11.16 (`backend/.venv`), `crewai==1.15.17`, `pytest==9.1.1`, `ruff==0.16.3`, `mypy==2.3.1`, real `ANTHROPIC_API_KEY` from `backend/.env`
- **Files changed**: `backend/src/app/flows/inquiry_flow.py` (`run_inquiry()`: manual `ThreadPoolExecutor` + `finally: pool.shutdown(wait=False)` instead of `with ...: shutdown(wait=True)`; shared `inquiry_id` generated per call and passed via `kickoff(inputs={..., "inquiry_id": ...})`; return dict now includes `inquiry_id`. `InquiryFlow.log_interaction`: uses `self.state.get("inquiry_id") or str(uuid.uuid4())` for the persisted row's `id`); `backend/src/app/persistence/interaction_log.py` (`record_interaction`: `INSERT` → `INSERT OR IGNORE`, same schema, same `id` PRIMARY KEY — no migration); `backend/tests/integration/test_inquiry_flow.py` (`test_inquiry_flow_end_to_end_per_scenario_category`: asserts per-`inquiry_id` row count instead of whole-db row count, to avoid a false-negative from an unrelated prior test case's un-killable straggler thread landing a row in a later test's `tmp_path` db — see "11. `run_inquiry()` double-log fix" for the full mechanism)
- **Root cause and fix rationale**: see "11. `run_inquiry()` double-log fix" above for the complete explanation, including why an upsert-style overwrite was rejected in favor of `INSERT OR IGNORE` (first-writer-wins preserves what the caller was actually told, per NFR-006 auditability) and why the test needed an `inquiry_id`-based assertion instead of a raw row count
- **Verification performed**: full `pytest` suite — **35 passed, 0 failed** (was 31 passed / 4 failed before this fix; all 4 previously-failing `test_inquiry_flow_end_to_end_per_scenario_category[...]` cases now pass, no regressions to the other 31); `ruff check src tests` clean on all files touched (same 2 pre-existing E501 nits in untouched predecessor files, unchanged); `mypy src` — `Success: no issues found in 16 source files`; direct live proof (bypassing pytest, real Anthropic API calls, `record_interaction` instrumented with a call-count/timestamp spy, `timeout_seconds=3` against a real ~14s full 5-agent flow, repeated for 2 scenarios): `run_inquiry()` returned in ~4.6s (not the full ~14-16s flow duration), `record_interaction` was called twice with the identical `inquiry_id` (timeout path + the abandoned straggler thread's own `log_interaction`, confirming it really did keep running), and exactly one row persisted in the database both times
- **Prohibited actions confirmed avoided**: no changes to `main.py`, `agents.yaml`, `config_loader.py`, the SAD §7 10s ceiling value, or the timeout-escalation payload/behavior; `interaction_log.py`'s schema (columns, table, PRIMARY KEY) is unchanged — only the `INSERT` statement's conflict behavior changed
- **Known gap flagged, not fixed this run (see Open Questions)**: `future.result(timeout=N)` in this Windows/`backend/.venv` dev environment consistently overshoots `N` by roughly 1.5-2s even for a trivial one-step `Flow` with no LLM calls — traced (via isolated reproduction) to CrewAI's own synchronous `rich`-based console/event-bus rendering on the background thread delaying the main thread's GIL scheduling, not to anything in this fix; harmless at the real 10s default (returns at ~10-12s instead of blocking the full ~15-16s) but means the timeout isn't sub-second-precise in this environment. Separately, the real reasoning-Crew's observed end-to-end latency (~15-16s) still exceeds the sad.md §7 10s ceiling on the happy path, meaning the escalate-on-timeout branch is now the *common* case for live traffic rather than a rare degradation path — this fix makes that branch behave correctly (fast return, single log row) but does not address whether the ceiling or the Crew's latency should be retuned; flagged for `@qa.eng`/stakeholder review alongside the still-pending §7 Latency Spike.

- **Timestamp**: 2026-08-20
- **Persona**: `@backend.eng`
- **Action**: `*implement-endpoint` follow-up — `GET /interactions`. Deliberate, narrow pull-forward: `sad.md` §4 defines `GET /interactions` ("interaction log for the ops view"), but `sad.md`'s own "MVP Build Sequencing" table scopes it to Phase 3. The operator explicitly authorized building this one endpoint now, ahead of that sequencing, to satisfy a reviewer comment requiring proof that `POST /chat` produces a real, visible interaction-log row before `@integration.eng` wires up the frontend — see §12 above for full rationale. No other Phase 2/3 endpoint was touched.
- **Resolved runtime**: `crewai` (`AAMAD_TARGET_RUNTIME=crewai`, consistent with `aamad.config.yml` `runtime.target: crewai` — no conflict)
- **Tools/versions used**: Python 3.11.16 (`backend/.venv`), `crewai==1.15.17`, `fastapi>=0.115`/`pydantic>=2.7` (unchanged), `pytest==9.1.1`, `uvicorn` (manual live server verification on port 8020), real `ANTHROPIC_API_KEY` from `backend/.env`
- **Files changed**: `backend/src/app/main.py` (added `GET /interactions` + `InteractionRecord` response model; updated module docstring to reflect the pull-forward); `backend/src/app/persistence/interaction_log.py` (docstring-only update to `list_interactions()`, no functional change); `backend/tests/integration/test_chat_endpoint.py` (extended with 2 new tests — see §12)
- **Verification performed**: full `pytest` suite — **37 passed, 0 failed** (35 baseline + 2 new tests), run twice to confirm stability against the environment's documented live-test flakiness (§11); live `uvicorn` server hit with real `curl`/direct-`run_inquiry` calls — `GET /interactions` `200 []` on an empty log, a real `POST /chat` call producing a timeout-escalation row correctly visible via `GET /interactions` with matching fields (including `diagnostic`, `null` classification/sentiment fields, and a real parsed `redaction_actions` array), and a second direct `run_inquiry(timeout_seconds=30)` call producing a full "responded" row with real `intent`/`confidence`/`sentiment_score`/`sentiment_label`/`match_found`/`grounded`/`response_text` all correctly visible via `GET /interactions`, both rows returned most-recent-first — see §12 for full detail
- **Prohibited actions confirmed avoided**: no `POST /email`, no `POST /escalations/{id}/resolve`, no review-queue endpoints, no query params/pagination/filtering on `GET /interactions`, no changes to `agents.yaml` or `inquiry_flow.py`

- **Timestamp**: 2026-08-21
- **Persona**: `@backend.eng`
- **Action**: `*develop-be` (Phase 2 of sad.md's "MVP Build Sequencing" — `channel=email`, `EscalationResolutionFlow`, review-queue write path) + `*implement-endpoint` (`POST /email`, `POST /escalations/{id}/resolve`) — see §13 above for full detail. Explicit operator decision to proceed without first running the mandatory §7 Latency Spike gate that would normally precede Phase 2 — noted again here and in Open Questions, not treated as a blocker per instruction.
- **Resolved runtime**: `crewai` (`AAMAD_TARGET_RUNTIME=crewai`, consistent with `aamad.config.yml` `runtime.target: crewai` — no conflict)
- **Tools/versions used**: Python 3.11.16 (`backend/.venv`), `crewai==1.15.17`, `fastapi>=0.115`/`pydantic>=2.7` (unchanged), `pytest==9.1.1`, `ruff==0.16.3`, `mypy==2.3.1`, `uvicorn` (manual live server verification on port 8030), real `ANTHROPIC_API_KEY` from `backend/.env`
- **Files changed/added**: `backend/src/app/persistence/review_queue.py` (new); `backend/src/app/persistence/interaction_log.py` (added `get_interaction_by_id`, no schema/behavior change to existing functions); `backend/src/app/flows/escalation_resolution_flow.py` (new — `EscalationResolutionFlow`, `run_escalation_resolution`, `OriginalInquiryNotFound`); `backend/src/app/main.py` (added `POST /email`, `POST /escalations/{id}/resolve`, `EscalationNotFoundError` + handler, reworded `ChatProcessingError`'s message, updated module docstring); `backend/tests/unit/test_review_queue.py` (new, 5 tests); `backend/tests/unit/test_interaction_log.py` (extended, +2 tests); `backend/tests/integration/test_escalation_resolution_flow.py` (new, 4 tests); `backend/tests/integration/test_email_endpoint.py` (new, 5 tests); `backend/tests/integration/test_escalation_resolve_endpoint.py` (new, 3 tests). `backend/src/app/flows/inquiry_flow.py` — **not** modified (confirmed already channel-agnostic by reading, §13.1).
- **Verification performed**: full `pytest` suite — **56 passed** (was 37 baseline + 19 new tests), run 3 times for stability (2/3 clean, 1 transient failure matching the already-documented §11 cross-test straggler-thread mechanism, unrelated to this run's own code — the one real bug this run introduced was caught deterministically by its own new tests before any live verification, see §13); `ruff check src tests` clean (same 2 pre-existing untouched-file nits only); `mypy src` — `Success: no issues found in 18 source files`. Live `uvicorn` server (port 8030, fresh db) hit with real requests: `POST /email` -> real `{reply_body, escalated}` (timeout-escalation path, `channel: "email"` confirmed via `GET /interactions`); a direct `run_inquiry(timeout_seconds=30)` call with a real angry message -> genuine sentiment-driven escalation (`reason: ["high_frustration"]`, not a timeout); `POST /escalations/{id}/resolve` called against both real escalated interaction ids -> both `200 {"status": "queued", "review_queue_id": ...}`; both resulting rows confirmed directly via `sqlite3` query against `backend/data/app.db`'s `review_queue` table (correct `original_inquiry_id` linkage, real `original_query_text`/`candidate_intent` inherited from the classified interaction); `404` (unknown `{id}`) and `422` (missing `/email` field) error paths both verified live. Test db removed after verification (gitignored, confirmed via `git status`).
- **Prohibited actions confirmed avoided**: no `GET /review-queue`, no `POST /review-queue/{id}/approve`, no `POST /review-queue/{id}/reject`, no read or write of `domain_config.json`/a live `knowledge_base` table anywhere in `escalation_resolution_flow.py` or `review_queue.py` (verified by inspection — neither module imports `app.domain.loader`), no changes to `agents.yaml`, `config_loader.py`, `reasoning_crew.py`, `pii_guard.py`, escalation thresholds, or the sad.md §7 10s ceiling
- **Known gap flagged, not fixed this run (see Open Questions)**: the §7 Latency Spike still has not been run as a proper 20+-request sample — this run's own live verification reproduced its already-known consequence (timeout-driven escalations on `/email`/`/chat`) again, consistent with every prior Phase-1 entry above.
- **Known gap flagged, not fixed this run**: the sad.md §7 timeout/latency mismatch (§11, already open) means `GET /interactions` will commonly surface timeout-escalation rows (mostly-`null` fields) rather than fully-populated "responded" rows in this environment until that gap is resolved — reproduced live during this action's own verification, not a defect in this endpoint.

- **Timestamp**: 2026-08-21
- **Persona**: `@backend.eng`
- **Action**: `*develop-be` Phase 3 of sad.md's "MVP Build Sequencing" (the live-KB migration behind `domain/loader.py::kb_search`, `backend/src/app/persistence/knowledge_base.py`) + `*implement-endpoint` (`GET /review-queue`, `POST /review-queue/{id}/approve`, `POST /review-queue/{id}/reject`) — the final backend build phase, sad.md §2's "Third, fully separate write path" (NFR-008). See §14 above for full detail.
- **Resolved runtime**: `crewai` (`AAMAD_TARGET_RUNTIME=crewai`, consistent with `aamad.config.yml` `runtime.target: crewai` — no conflict)
- **Tools/versions used**: Python 3.11.16 (`backend/.venv`), `crewai==1.15.17`, `fastapi>=0.115`/`pydantic>=2.7` (unchanged), `pytest==9.1.1`, `ruff==0.16.3`, `mypy==2.3.1`, `uvicorn` (manual live server verification on port 8040), real `ANTHROPIC_API_KEY` from `backend/.env`
- **Files changed/added**: `backend/src/app/persistence/knowledge_base.py` (new — live, mutable KB store: `count_entries`, `seed_from_domain_config`, `insert_kb_entry`, `list_kb_entries`); `backend/src/app/domain/loader.py` (`kb_search` updated to read from the live `knowledge_base` table instead of `config.knowledge_base` — ADR-005's scoring/floor/top-3 algorithm unchanged; module docstring extended); `backend/src/app/persistence/review_queue.py` (added `get_review_queue_entry`, `update_review_queue_status` — no schema change; `list_review_queue`'s docstring updated to reflect it now backs `GET /review-queue`); `backend/src/app/main.py` (added `GET /review-queue`, `POST /review-queue/{id}/approve`, `POST /review-queue/{id}/reject`, `ReviewQueueNotFoundError`/`ReviewQueueConflictError`/`InvalidApprovalError` + handlers, updated module docstring); `backend/tests/unit/test_knowledge_base.py` (new, 8 tests); `backend/tests/integration/test_review_queue_endpoints.py` (new, 11 tests).
- **Verification performed**: full `pytest` suite — **75 passed** (was 56 baseline + 19 new tests: 8 + 11), run 3 times for stability, all 3 runs clean at 75/0 (post-session stderr noise from the already-documented un-killable straggler-thread characteristic appears after pytest's own summary line in every run — not a failure, does not affect the reported count); `ruff check src tests` clean on every file touched/added (the previously-flagged `domain/loader.py:97` pre-existing nit no longer applies — this phase legitimately modified that file and the nit was fixed inline rather than left, since the file is no longer untouched predecessor code; the 1 remaining pre-existing nit, `task_outputs.py:44`, is in a file untouched this phase); `mypy src` — `Success: no issues found in 19 source files` (was 18; +1 new module). Live `uvicorn` server (port 8040, fresh db) hit with real requests — the full AC-011 chain: real `POST /chat` -> real escalation -> `POST /escalations/{id}/resolve` -> `GET /review-queue` confirms `status: "pending"`; `POST /review-queue/{id}/approve` with real keyword overrides (required, since the queued candidate had `candidate_intent: null` and `candidate_keywords: []`) -> `200` returning the new KB entry; a direct `kb_search()` call and a separate, fresh `run_inquiry(timeout_seconds=30, ...)` call (worded to match the new entry's keywords) both confirm the new entry is retrievable and grounds a real composed reply — `GET /interactions` shows `match_found: true`, `grounded: true`, `outcome: "responded"` for that inquiry, live, same process, no restart; a second real (sentiment-driven, non-timeout) escalation was resolved and rejected via `POST /review-queue/{id}/reject`, confirmed no KB write occurred; `GET /review-queue` afterward showed correct, distinct statuses (`"approved"`, `"rejected"`) for both candidates. Live-verification db removed after checks (gitignored, confirmed via `git status`).
- **Prohibited actions confirmed avoided**: no write path or mutable store added for `taxonomy` (still fully static, read directly off `DomainConfig`); no changes to `agents.yaml`, `config_loader.py`, `reasoning_crew.py`, `pii_guard.py`, `escalation_resolution_flow.py`, escalation thresholds, or the sad.md §7 10s ceiling; ADR-005's scoring/floor(0.20)/top-3 algorithm left byte-for-byte unchanged — only `kb_search`'s entry data source moved.
- **Known gap flagged, not fixed this phase (see Open Questions)**: the §7 Latency Spike still has not been run as a proper 20+-request sample — now unblocked (all 3 backend build phases are code-complete) but genuinely not executed this run, carried forward as the top follow-up for whoever runs Deliver-phase QA. `POST /review-queue/{id}/approve` does not validate that an overridden `intent` matches a real `taxonomy[].intent` value, and does not validate keyword quality/retrievability — both would silently produce an unretrievable/dead KB entry rather than erroring, flagged for a future Reviewer-UI hardening pass, not required by sad.md for MVP.
